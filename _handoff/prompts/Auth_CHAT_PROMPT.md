# Cursor Chat Prompt: Auth

Роль: реализатор Auth (регистрация/логин) для AEVION.

Контекст:
- Сейчас Auth не реализован.
- QRight/QSign/QTrade работают без авторизации.

Цель:
- Добавить регистрацию/логин и связать владение/доступ к QRight объектам.
- Минимально: создать пользователя, залогинить и обеспечить, что UI показывает только нужные записи.

Ограничения:
1. Не меняй базы/стек.
2. Не ломай текущие страницы (сделай обратную совместимость, если нужно).
3. Каждая правка делается маленькими шагами, после каждого шага выполняется тест.

Тесты (минимум после внедрения):
- `POST /api/auth/register` -> 201
- `POST /api/auth/login` -> 200
- Страница QRight показывает данные пользователя (после релогина).

