import type { Metadata } from "next";

/* Запрет индексации: переход по личной ссылке приглашения.
 *
 * В адресе — ТОКЕН. Попадание такого адреса в поисковый индекс означает, что
 * доступ становится виден любому, кто прочитает выдачу.
 *
 * Прямой аналог — `bank/r/[code]` и `qpaynet/r/[token]`: обе такие страницы уже закрыты от индексации, и по той же причине.
 *
 * Найдено 30.08.2026 обходом всех адресов с секретом в пути: из двенадцати
 * таких страниц шесть были без запрета. Класс, стало быть, известен и
 * закрывался по одной, а не разом.
 *
 * `follow: true` намеренно: запрещён показ САМОЙ страницы, обход ссылок с
 * неё не мешает. */
export const metadata: Metadata = {
  robots: { index: false, follow: true },
};

export default function BuildReferralLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
