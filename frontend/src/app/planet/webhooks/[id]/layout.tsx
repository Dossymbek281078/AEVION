import type { Metadata } from "next";

/* Запрет индексации: настройка вебхука: рабочий экран владельца, содержит идентификатор его интеграции.
 *
 * ПОЧЕМУ ЭТО НЕ ЗАВИСИТ ОТ РЕШЕНИЙ О ПОИСКЕ. Остальные страницы с
 * идентификатором в пути — сертификаты, авторы, артефакты — вопрос открытый:
 * их, возможно, СТОИТ показывать в выдаче как доказательство. Здесь вопроса
 * нет: в адресе лежит ТОКЕН ДОСТУПА, и попадание такого адреса в поисковый
 * индекс означает, что доступ утёк любому, кто прочитает выдачу.
 *
 * Заведено 30.08.2026 при разборе страниц, наследующих чужой canonical.
 * Наследование им безвредно — они и не должны быть в поиске, — но раньше это
 * держалось на том, что поисковик до них не дойдёт. Теперь запрещено прямо.
 *
 * `follow: true` оставлен намеренно: ссылки СО страницы обходить можно,
 * запрещён показ САМОЙ страницы. */
export const metadata: Metadata = {
  robots: { index: false, follow: true },
};

export default function PlanetWebhooksLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
