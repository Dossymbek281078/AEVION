# AEVION Fan — веерные скидки + двадцать два живых дефекта на пути к деньгам

Ветка `feat/fan-discounts` → `main` · **76 коммитов** · backend tsc 0, frontend tsc 0 · backend `npm run test:stable` **1359 PASS / 0 FAIL**, frontend **488 PASS** · `next build` — пересборка запущена после последних правок фронтенда, результат ниже · pricing-смоук **17/21 против прода** (4 красных — ровно то, что ветка добавляет; 21/21 после мержа), checkout-rails **17/17**, канарейки **3 PASS** (условия веера, дрейф LS-цен, снятые цены на витринах) · мутационный аудит **17 мутаций, 15 пойманы, 2 нейтральны и разобраны**.

**Про плавающий красный в чужой зоне — он не от этой ветки, и это измерено.**
`tests/devhub-integrations.test.ts` падает **нестабильно и каждый раз другим тестом**:

| прогон | результат |
|---|---|
| 1 | зелёный целиком |
| 2 | 1 красный — `tts step: R2 env missing` |
| 3 | 3 красных — `merge sha`, `tts R2 set`, `SSE music` |
| 4 | 1 красный — `opens a PR on a new branch` |

В одиночку файл стабильно даёт **191 PASS**, в паре с новым тестом этой ветки — **194 PASS**.
То есть это его собственная нестабильность под параллельной нагрузкой (async-хвост,
из-за которого мок доживает до следующего файла), а не регрессия отсюда. DevHub — чужая
зона, ветка его не трогает; диагноз передан владельцу. Если увидишь красный в CI — сверь
имя теста: если оно каждый раз новое и все они из `devhub-integrations`, он не отсюда.

Двадцать два дефекта в таблице ниже — не переписанный код, а найденное по пути: каждый из них уже работал на живой монетизации и ни один не падал, поэтому ни один не был виден.

## Зачем

Механика Higgsfield, замеренная через их живой биллинг 2026-07-26 (не по памяти — фактический ответ их pricing-API): одна покупка не выдаёт один товар, она *радиирует* по каталогу — разной глубиной, на ограниченный срок, и список того, что откроется, показан **до** оплаты. У них это «365-DAY UNLIMITED» на 7 моделей + «7-Day Unlimited» на 2-3 горячие + лестница объёма 39→42→44%.

У AEVION 43 модуля в прайсе и другая экономика: нет амортизированного GPU-парка, есть реальный per-token/per-render COGS. Поэтому веер здесь — **скидка на соседние модули**, а не «unlimited».

## Механика

```
ring 1  прямой комплемент   −30% → до −45%   один кластер (BUNDLES + 12 явных) или ≥2 общих тега
ring 2  тот же домен        −15% → до −30%   ≥1 общий тег в projects.ts
ring 3  остальная планета     0%             прайс без скидки
```

Уровень веера = число купленных платных модулей (1..5), каждый добавляет +5 п.п. кольцам 1-2. Окно — 14 дней от покупки, продлевается новой покупкой. Скидка фиксируется на оплаченный период.

**Четыре потолка, все обязательные:** потолок кольца → общий 50% (тот же `MAX_PROMO_DISCOUNT_RATIO`, что у промокодов — намеренно один number, два независимых разъехались бы) → 30% для 15 модулей с переменным COGS (оба гейта `QCOREAI_*_QUOTA` до сих пор dormant, per-модульный COGS не измерен) → совместный «веер + промо ≤ 50%», без которого «промо −50% + веер −45%» давало заказ дешевле нуля.

**ring 3 = 0% по замеру, а не по вкусу:** при уровне 1 в ring3 падало 27 модулей из 31, и «−10% на всё» превращало прайс в фикцию. Замер это показал, тесты — нет.

## 🔴 Что нашлось по пути (всё живое, всё в этой ветке починено)

| Баг | Как это выглядело | Проверка |
|---|---|---|
| **Скидки не доходили до счёта** на LemonSqueezy и Gumroad: `createIntent` не передавал ни `custom_price`, ни `discount_code`, а Gumroad-URL цену выразить не может | смета показывала «AEVION20 −$17.80», покупатель уходил на чекаут и платил полную цену, в логах тишина | прогнано вживую: на Gumroad-канале ответ теперь `quotedUsd: 45.2 / chargedUsd: null / discountHonoured: false` + warn в лог |
| **Промо считалось двумя способами**: `buildQuote` округлял до доллара, `checkout.ts` — до цента | Full+AEVION20: смета обещала итог $71, чекаут выставлял $71.20 — при заявленном в коде инварианте «quote == charge» | одна функция `computePromoDiscountUsd()`; чекаут больше не считает сам |
| **`/api/pricing/quote` не умел флагманский тариф**: `pro` отсутствовал в валидаторе | `400 invalid_tier` на Universe $249.99 — **воспроизведено на проде прямо сейчас** | после мержа: `total=2499.9 discount=499.98` |
| **Окно веера обнулялось каждым продлением**: `subscription_updated` в `ACTIVATE_EVENTS`, dedup-ключ включает `renews_at` → продление писало новую подписку со свежим `ts` | активный подписчик получал −30% навсегда, 14-дневный дефицит исчезал | `fanAnchorAt` + 9 тестов; дискриминирующая сила проверена (снял фикс — падают ровно нужные тесты) |
| **Welcome-письмо на каждом продлении** (тот же корень) | покупатель получал «Добро пожаловать в AEVION» ежемесячно, при том что чек присылает сам процессинг | письмо только на новой покупке; продление → `emailSkipped: "renewal"` |
| **Покупатель Universe назывался «Lite»** в письме (`TIER_DISPLAY.pro = "Lite"`) | подписчик за $249.99 получал «Добро пожаловать в AEVION Lite» | тест-регрессия |
| **Веер был слеп к поштучным покупкам**: покупки через LS-варианты `app_*` (9 штук) живут в таблице `AppSubscription`, а `/fan/me` читал только JSONL | купивший CyberChess и QContract поштучно получал «веер включается после первой покупки» — механика молчала ровно для тех, ради кого сделана, и врала им ПОСЛЕ оплаты | `lib/ownedModules.ts` — один читатель на два стора + 5 тестов на маппинг слагов; при недоступной базе честный `appsSource: "unavailable"` |
| **Панель залипала в тупике** при разовой ошибке базы: `/fan/me` отдавал «данные неполные» → «уровень 1 · 0 модулей» И скрытый ручной выбор | покупатель с купленными модулями оставался без веера и без единого пути к нему; перезагрузка не помогала | ретрай на обоих слоях + правило «неполные данные не выдаются за пустой веер»; 4 теста, найдено живым прогоном |
| **Плейсхолдер `{cur}` уехал в интерфейс**: `usePricingT` подставлял через `String.replace` (только первое вхождение) | в стене 402 покупатель видел «$12.35/мес вместо **{cur}**19»; касалось всей pricing-локализации | `split/join` + 4 теста по всему словарю |
| **Кривое тело роняло ПЛАТЁЖНЫЙ эндпоинт в 500**: `ownedModules: [42]`, `modules: 42`, `promoCode: 42` — `.trim()`/`for…of` по не-строке | любой кривой или злонамеренный клиент получал 500 на пути к оплате; первое — моё, два других лежали годами | валидация в двух слоях (ручка + функция) + прогон мусором по всем полям четырёх ручек |
| **Подписка провижинилась на ЛЮБУЮ строку в email** | в сторе оказались записи с адресом `'; drop table users; --`, нулевыми байтами и строкой в 5000 символов | та же проверка, что в `/api/pricing/lead`; тест: 6 мусорных адресов не оставляют ни строки |
| **Разовый сбой базы навсегда выключал метрику** неприменённых скидок (одноразовый флаг + пустой `catch` на вставке) | один блип на старте — и расхождений «обещали/списали» не видно весь срок жизни процесса | кулдаун вместо одноразового флага, ошибки вставки в лог; та же дыра помечена в `paywallDenyLog` |
| **Двойной учёт в метрике**: `charge()` писал запись до вызова провайдера, а каскад пробует их по очереди | один запрос попадал в метрику 2-4 раза — за каналы, которые ничего не отдали; цифра ломалась ровно при сбое провайдера | запись перед самым ответом, идемпотентно + тест |
| **Заказ на ноль не попадал в метрику вовсе** | самые крупные скидки были невидимы в метрике, которая их считает | запись как сессия с `honoured: true` |
| **Прототипные ключи в `currency`** (`in` вместо белого списка) | `currency: "constructor"` → HTTP 200 со сметой, где `total`/`subtotal`/`unitPrice` = `null` — пустые цифры там, где человек смотрит цену | `parseCurrency()` во всех трёх ручках; тот же класс починен в `moduleForAppSlug` |
| **🔴 Подписчику Universe продавали то, за что он уже заплатил** | флагман за $249.99 видел **31 модуль к докупке** — ровно как бесплатный пользователь. `includedIn` ни у одного модуля не содержит `pro` (тариф всеобъемлющий через `limits.modules: null`), а код сравнивал сырой `tier.id` | взято ГОТОВОЕ правило пейвола `planGate.normalizeTier`, а не заведено второе представление о составе тарифа. После: pro/full/enterprise — 0 к докупке |
| **🔴 Разовый сбой сети выключал чип цены навсегда** | кэш клал промис в память до его разрешения; один холодный старт бэкенда — и веерная строка не появлялась уже никогда, а у `/api/pricing` пропадала **сама цена**. Лечила только перезагрузка вкладки | общий `cachedFetch` с кулдауном 30 с. Тот же тупик уже чинился в `FanDiscountPanel` — здесь остался незамеченным |
| **Витрины обещали цены, снятые 22 июля** — 21 место в трёх локалях + 3 OG-карточки + инвесторская `/pitch` | страница обещала $19, чекаут списывал $24. «$190 вместо $228» при фактических $240/$288. На одной карточке одновременно «37 модулей» и «27 модулей» | всё приведено к `data/pricing.ts` + канарейка `scripts/public-price-drift.js` (в `all-smokes`). Она же нашла 13 мест, которые я пропустил глазами |
| **🔴 Stub выписывал платную подписку без оплаты** — при незаданных процессингах чекаут падал в stub и провижинил настоящую подписку | прогон дал три записи в сторе: lite, medium и **Universe с `amountUsd: 249.99`** — отчёт о выручке считает эти деньги пришедшими. Достижимо не гипотетически: у Universe нет LS-варианта вовсе, это записано в `lemonSqueezyVariants.ts` | в проде 503 + причина + ссылка на связь, подписка не выписывается; вне прода прежнее поведение (без stub не поработать без ключей). Мутация: снятие гарда роняет тест |
| **🔴 Веерная скидка давалась ПО ЗАЯВЛЕНИЮ клиента** — `ownedModules` и `lastPurchaseAt` шли из тела запроса прямо в расчёт списания | анонимный запрос с «я владею вот этими пятью» давал Medium+3 модуля за **$59.35 вместо $76** (−22%). Ни авторизации, ни сверки со стором. Мой дефект — я же и внёс `ownedModules` в чекаут | владение резолвит СЕРВЕР: JWT (алгоритм пиннуется) → иначе email подписки → `readOwnedModules`, тот же источник, что у `/fan/me`. Нет личности или стор недоступен → веера нет. В ответе `fan.ownershipSource`. Проверено прогоном: атака $76, реальный владелец $63.05 со скидкой $12.95 |
| **`?limit=abc` отдавал пустой список вместо ста записей** — в четырёх админских списках (лиды, промо, партнёры) | `Math.max(NaN, 1)` — это `NaN`, а не 1, и `.slice(0, NaN)` даёт `[]`; `?? "100"` ловит только `undefined`. Админ видел «лидов нет» вместо данных | `parseLimit()` с `Number.isFinite` до зажима; тест на обе половины — мусор даёт фолбэк **и** реальные значения проходят. Прогреп репозитория: ещё 8 таких мест в чужих зонах, переданы через `AEVION_COORDINATION.md` |
| **`SUBSCRIPTIONS_FILE` в тестах не работал**: путь биндился в module-scope, а `import` в ESM поднимается выше присваивания env | `tests/paywallProvisionFlow` краснел и на чистом main; в рабочей копии копилось **101** мусорная тестовая подписка | путь читается лениво; тест зелёный, стор не засоряется |

## Что появилось

**Бэкенд**
- `data/fanDiscounts.ts` — движок (кольца, лестница, потолки, окно, витрина). Единственная точка, где веер попадает в деньги, — `buildQuoteWithFan()`; и `/quote`, и чекаут зовут её, поэтому «смета == списание» держится по построению.
- `POST /api/pricing/fan`, `GET /api/pricing/fan/preview`, `GET /api/pricing/fan/me`.
- `GET /api/pricing/checkout/discount-integrity?days=N` — сколько скидок пообещали и не применили. Postgres (`discount_integrity_log`) по паттерну `lib/paywallDenyLog.ts`, при недоступной базе — счётчики в памяти с честным `source: "memory"` + `bootedAt`.
- `scripts/ls-variant-price-drift.js` — канарейка инварианта «цена LS-варианта == цена в `data/pricing.ts`», который держался на комментарии.

**Фронтенд**
- `FanDiscountPanel` на `/pricing`: авторизованному покупателю веер приходит с сервера (`/fan/me`, оба стора), гостю остаётся витрина «что будет, если купить». Предложения кликабельны и ведут на `/pricing?module=…`. Размечено четырьмя событиями (`fan_view`, `fan_owned_pick`, `fan_offer_click`, `fan_terms_open`) — иначе окно 14 дней и глубину нечем проверить; веерная строка в `ModulePricingChip` (28 витрин); веерное предложение в стене 402; «цена по запросу» для 8 модулей без прайса → лид с названием модуля.
- Paywall переведён в светлую схему. Акценты пересчитаны: старые давали на белом 1.92–4.23:1 при норме AA 4.5:1, новые 4.76–7.10:1.
- Копирайт — `lib/pricingI18n/sections/fan.ts` (ru/en), не хардкод.

## Покрытие тестами

82 теста в 9 файлах закрывают механику, окно, потолки, письмо, слияние двух сторов покупок, согласованность цен и локализацию. У каждого блока проверена **дискриминирующая сила** — возврат прежнего поведения роняет ровно те тесты, которые должен. Таблица — `docs/FAN_DISCOUNTS_2026-07.md` §7о.

## Условия и защита от дрейфа

- Раздел **«Веерная скидка»** в оферте: `/pricing/refund-policy#fan` (в существующей юридической странице, ссылка из панели). Дедлайн без описанных условий — спор, который мы проиграем.
- `npm run check:fan-terms` — канарейка «оферта == константы движка», падает в обе стороны. Проверена подменой 45%→40%.
- `npm run check:ls-price-drift` — канарейка «цена LS-варианта == цена в коде». Без ключа SKIP + честное «это не значит, что расхождений нет».
- Обе заведены в `scripts/all-smokes.js`.

## Приёмка после мержа

1. `node aevion-globus-backend/scripts/pricing-prod-smoke.js` — сейчас на проде **17/21**, четыре красных ровно те, что эта ветка добавляет (`/fan`, `/fan/preview`, `/fan/me`, `quote tierId=pro`). После деплоя должно стать 21/21.
2а. Витрина модуля и аналитика проверены на ПРОД-сборке: на `/qcontract` чип показывает «веер: 5 модулей дешевле на $30.3/мес», событие `fan_view` (`source=module-chip/qcontract`) реально пишется в `events.jsonl`.
2. ~~Открыть `/pricing` и посмотреть панель веера~~ — **уже проверено в браузере** (§7з): панель, предложения с ценами из движка, мобильная вёрстка при 360px (ноль переполнений), оферта `#fan`, стена 402 в светлой схеме (`rgb(255,255,255)` / `rgb(15,23,42)`).
3. Прогнать `npm run check:fan-terms` — оферта не должна разойтись с константами.

## Требует руки основателя

- **Мерж.**
- **`LEMON_SQUEEZY_ALLOW_CUSTOM_PRICE=1` на Railway** (или offer-коды в LS/Gumroad) — иначе скидки остаются реальными только на PayPal/PayBox, и API про это честно говорит.
- **🔴 LS-вариант для Universe (`pro`, $249.99).** Его нет вовсе — `lemonSqueezyVariants.ts` прямо пишет, что «pro-чекаут проваливается в Gumroad/stub, минуя LS». До этой ветки такой чекаут в проде выписывал бы подписку **без оплаты**; теперь он отвечает честным 503 и ведёт на связь. Флагманский тариф остаётся непродаваемым автоматически, пока вариант не заведён на дашборде LS — это твоя рука, не код.
- **Решения о цене**, которые ветка НЕ принимает: 8 модулей без à-la-carte цены; `fintech-suite` обещает 8%, даёт 4.8% ($79 против суммы $83); годовая скидка 16.7% против замеренных 23% у Higgsfield. Ни одна цена не изменена — `PRICING_STRATEGY_2026-07.md` запрещает репрайсинг без бенчмарка.

## Не в этой ветке (сознательно)

- Per-модульный COGS: **невозможно** существующими данными — `QCoreTokenLedger` (`ensureQCoreTables.ts:465`) хранит только `userId`/`ym`/`tokensIn/Out`/`premium*`, ни модуля, ни модели. Пока не измерено, потолок 30% поднимать нечем.
- Веер в DevHub-флоу — зона `frontend-qcore` по `AEVION_COORDINATION.md`.
- `tests/devhub-integrations.test.ts` флакует под параллельной нагрузкой (счёт падений 4→3→1→0 при неизменном коде, в изоляции 191/191 дважды). Гигиена моков и env исключена чтением; детерминированного воспроизведения нет. Чужая зона — оставлено владельцу как факт, не как догадка.

Обоснование параметров, замеры и открытые пункты — `docs/FAN_DISCOUNTS_2026-07.md`.
