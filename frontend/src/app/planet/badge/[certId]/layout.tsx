import type { Metadata } from "next";

/* Значок сертификата реестра: страница для ВСТРАИВАНИЯ, а не для чтения.
 *
 * Её адрес дают, чтобы вставить картинку к себе на сайт или в README. Человек
 * по нему не приходит и в поиске её не ищет. В выдаче она соревновалась бы со
 * страницей самого объекта, показывая при этом одну картинку вместо содержания.
 *
 * ГРАНИЦА, и она важна: закрывается значок КОНКРЕТНОГО объекта — тот, у
 * которого идентификатор в адресе. Страницы-ГЕНЕРАТОРЫ значков
 * (`bank/badge`, `api-explorer/badges`) НЕ закрыты и закрывать их нельзя: это
 * посадочные, объясняющие, как значок получить, и их как раз ищут.
 *
 * Замер 30.08.2026: страниц со словом badge семь, генераторов из них два.
 * Образец найден группировкой списка наследующих canonical по разделам —
 * плоский список этого не показывал. */
export const metadata: Metadata = {
  robots: { index: false, follow: true },
};

export default function PlanetBadgeLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
