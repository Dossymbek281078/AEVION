/**
 * Ключи локального прогресса тренажёра — один экземпляр строки на всех.
 *
 * Зачем отдельный файл: дашборд читал `aevion-smeta-quiz-best-v1`, а квиз
 * писал `smeta-trainer:resource-quiz-pass-v1` — и ЛУЧШИЙ РЕЗУЛЬТАТ не писал
 * никто. Ключ, у которого есть читатель и нет писателя, выглядит рабочим:
 * дашборд молча показывает значение по умолчанию (0 из 20, 0 %), плитка
 * квиза никогда не заполняется, опыт за квиз не начисляется, а достижение
 * «Снайпер квизов» (95 %+) недостижимо в принципе.
 *
 * Держать строку в двух местах — тот же класс: разойдутся при первом же
 * переименовании, и разойдутся молча.
 */

/** Лучший результат квиза: `{ scoreCorrect, scoreTotal, weakCategories? }`. */
export const KEY_QUIZ_BEST = "aevion-smeta-quiz-best-v1";

/** Факт прохождения квиза по ресурсам — только флаг, без результата. */
export const KEY_RESOURCE_QUIZ_PASS = "smeta-trainer:resource-quiz-pass-v1";

/** Событие, по которому дашборд перечитывает прогресс. */
export const PROGRESS_UPDATE_EVENT = "aevion-smeta-progress-update";
