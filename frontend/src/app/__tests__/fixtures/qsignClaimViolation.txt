Fixture for qsignClaims.guard.test.ts — do not "fix" these lines.
They are the exact wording that shipped to buyers while production
answered {"qsign":{"mode":"preview","reason":"seed_unset"}}, kept here
so the guard's matcher is proven against the real defect.

proof: "ML-DSA-65 GA · prod smoke 20/20 · QShield Lagrange-reconstruct"
<li>signatures must be reissued today (ML-DSA-65, FIPS 204, in prod).</li>
note: "ML-DSA-65 FIPS 204 в production — единственный в регионе"
desc: "Enterprises are starting to require post-quantum signatures. We already ship it."
Ours will. Only B2C product with ML-DSA-65 on the shelf.
{ title: "Post-quantum on day 1", desc: "No one else ships ML-DSA-65 B2C." }
ML-DSA-65 FIPS 204 GA — only commercial product of this type in the region.
<div className="card-label">In production · QSign</div>

--- добавлено 28.08.2026: по строке на каждый новый шаблон ---
ML-DSA-65 is live in QSign v2 today.
Постквантовая подпись работает на проде прямо сейчас.
FIPS 204 включен и подписывает каждый сертификат.

