/**
 * Дата публичного запуска CyberChess — ОДНА на весь модуль.
 *
 * Почему константа, а не число в каждом месте. До 01.09.2026 дата стояла
 * литералом в трёх файлах, и один из них отстал: страница запуска считала
 * от 30 СЕНТЯБРЯ (верно — основатель перенёс запуск 29.08), а главная
 * страница модуля — от 30 августа. Следствие было тихим и дорогим: блок
 * «Написать вам, когда откроются турниры?» скрывается ПОСЛЕ дня запуска,
 * и на главной он исчез 31 августа — за месяц до срока. То есть весь
 * сентябрь, пока идёт тестовая версия, модуль не собирал бы адреса вовсе,
 * и никакой ошибки при этом не показывал.
 *
 * Месяцы в Date.UTC считаются с нуля: 8 — это сентябрь.
 */
export const CHESS_LAUNCH_UTC = Date.UTC(2026, 8, 30);

/** Человеку — словами, чтобы подпись и расчёт не разошлись. */
export const CHESS_LAUNCH_HUMAN = "30 сентября";
