# HANDOFF — 2026-04-30

> Снимок сессии для продолжения с другого ноутбука.

## Что в этом worktree

- **Код тренажёра** в `frontend/src/app/smeta-trainer/` — рабочий MVP P1, открывается на `/smeta-trainer`
- **Курс полностью** в `course/` — 73 markdown-файла (программа 5 уровней, методички, кейс, LMS, глоссарий, тесты, шаблоны куратора). Это база контента; не перепутать с кодом.
- **Memory-бэкап** в `.claude-memory-backup/` — файлы из `~/.claude/projects/.../memory/`. На новом ноуте скопировать обратно (см. ниже).

## Что было сделано в этой сессии (хронологически)

1. **Курс «Сметное дело в РК»** — собран целиком в `course/`:
   - Программа 5 уровней: с-нуля → пользователь → ПТО → проектировщик → эксперт
   - Методички уроков на каждый уровень (6-8 уроков на уровень)
   - Сквозной кейс через все уровни — капремонт школы №47, Алматы
   - LMS-структура, глоссарий 100+ терминов, банк 250 тестов, шаблоны куратора
2. **CyberChess perf fix** — отдельная ветка `chess-tournaments`, коммит `f8f2a57`. translate3d ghost + cached rect hit-test + snap-to-center. К текущей сессии не относится напрямую, но было сделано в её рамках.
3. **AI-тренажёр сметного дела** — этот worktree, ветка `feat/smeta-trainer`, коммит `ba5a6ab`:
   - 14 файлов скелета (P1)
   - 10 расценок, 3 индекса, школа №47 как учебный объект с геометрией
   - Расчётный движок (calc.ts) с разнесением по ФОТ/ЭМ/материалы и индексами
   - AI-сценарий «забыл вычесть проёмы»
   - UI: 3 колонки (поиск + таблица + AI-панель)

## Главное на чём остановились

**Пользователь дал критическую обратную связь после P1:**

> «Сметы на самом деле выглядят по другому совершенно в реальности в Казахстане. То что сейчас там, это не обучение сметам в Казахстане, это обучение чему то.»

**Признано:** мой UI — это обобщённая табличная сетка, а не воспроизведение настоящей казахстанской ЛСР.

**План на следующую сессию (P2):**
1. **Сначала — собрать референсы**. Реальные PDF/скриншоты:
   - ЛСР казахстанской формы (точная редакция)
   - КС-2 / КС-3
   - ССР по 12 главам
2. Через `WebSearch` / `WebFetch` пройтись по открытым тендерам РК (госзакупки kz публикуют сметы как часть конкурсной документации) если пользователь не предоставит свои
3. **Полностью переписать UI ЛСР** под реальную форму:
   - Шапка с реквизитами (объект, базисная/текущая стоимость, СНБ-редакция, средства на оплату труда, расчётный измеритель)
   - Таблица с колонками: № п/п, Шифр, Наименование, Ед.изм., Количество, Цена за единицу (с разбивкой ОТ/ЭМ/в т.ч. ОТМ/МР), Общая стоимость (та же разбивка), Затраты труда рабочих
   - Подвал с НР+СП по разделу, итогами, лимитированными
   - Подписи: Составил / Проверил / Согласовано / Утверждаю
4. После ЛСР — КС-2, КС-3, ССР с теми же принципами

**Структурно:** калькулятор и AI-сценарий уже работают и не перерабатываются. Меняется только VIEW — рендер таблицы и форм.

## Состояние рабочих файлов

```
C:\Users\user\aevion-smeta-trainer\        ← этот worktree, ветка feat/smeta-trainer
  ├── CLAUDE.md                            ← правила сессии, P1-P9 roadmap
  ├── HANDOFF-2026-04-30.md                ← этот файл
  ├── course/                              ← 73 файла курса (был отдельно в C:\Users\user\smeta-rk-kurs\)
  ├── .claude-memory-backup/               ← memory-файлы для переноса
  ├── frontend/src/app/smeta-trainer/      ← рабочий код P1
  └── (остальное от AEVION-monorepo)
```

## Кросс-зависимости

- **AEVION QCoreAI** (`/api/qcoreai/*`) — будет использоваться для AI-сценариев в P3+
- **Другие AEVION-окна** — не затрагиваем, см. раздел «Границы сессии» в `CLAUDE.md`

## Что НЕ закоммичено / не запушено

Всё, что было сделано в этой сессии — **запушено**.

- `feat/smeta-trainer` коммит `ba5a6ab` (P1 скелет тренажёра) — на origin
- `chess-tournaments` коммит `f8f2a57` (perf fix CyberChess) — на origin
- Memory-обновление `feedback_default_yes.md` — только в `~/.claude/...` (теперь и в `.claude-memory-backup/` этого worktree)

## Команды для нового ноутбука

См. в конце сессии — пользователь запросил PowerShell-команду для бутстрапа.
