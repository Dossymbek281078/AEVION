import type { Metadata } from "next";

/**
 * Метаданные страницы живут здесь, а не в page.tsx: сама страница помечена
 * "use client", а клиентский компонент не может экспортировать metadata —
 * директива обязана быть первой строкой файла. Тот же приём, что у /apps.
 *
 * Страница перенесена из невлитой ветки 28.08.2026 и наследовала общий заголовок
 * сайта; сторож `pageMetadata.guard` это и поймал.
 *
 * Заголовок и описание намеренно НЕ обещают победы: страница честно называет,
 * где конкуренты сильнее, и обещание в заголовке разошлось бы с содержимым —
 * человек пришёл бы за «мы лучше», а нашёл таблицу с нашими слабыми местами.
 *
 * ⚠️ `tsc` на нарушении этого правила ЗЕЛЁНЫЙ: это правило сборки Next.js, а не
 * системы типов, и ловится только настоящим next build или этим сторожем.
 */
export const metadata: Metadata = {
  title: "AEVION и аналоги: что сравнили по фактам",
  description:
    "Построчное сравнение с Lovable, Bolt.new, Replit, v0 и Supabase — с указанием, где сильнее они. В таблицу попадает только то, что измерено; остальное названо как непроверенное.",
  alternates: { canonical: "https://aevion.app/compare" },
  openGraph: {
    title: "AEVION и аналоги: сравнение по фактам",
    description:
      "Где мы измерили, там числа; где сильнее конкурент — так и написано. Строка без замера в таблицу не идёт.",
    url: "https://aevion.app/compare",
    type: "website",
  },
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
