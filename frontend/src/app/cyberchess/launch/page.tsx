import type { Metadata } from "next";
import { getApiBase } from "@/lib/apiBase";
import { channelFrom, keepChannel } from "@/lib/products";
import { daysUntilLaunch } from "@/lib/daysUntilLaunch";
import { WaitlistCapture } from "@/components/WaitlistCapture";
import { LandingView } from "@/components/LandingView";
import { PageTracking } from "@/components/PageTracking";

// Посадочная страница запуска CyberChess — 30 августа 2026.
//
// ЗАЧЕМ ОТДЕЛЬНАЯ СТРАНИЦА. Ролики ведут человека в шахматы, а /cyberchess —
// это само приложение: доска на весь экран, и место для «оставьте адрес» там
// отнимало бы игровое поле. Форма на главной и на /go собирает адреса всей
// платформы; здесь собираются те, кто пришёл именно за шахматами, и поле
// source="cyberchess" потом отвечает, какой канал их привёл.
//
// ЧЕСТНОСТЬ ЧИСЕЛ. Размер банка задач берётся из живого API, а не зашивается:
// на 18.08 в базе 502 584 задачи, и любое зашитое число начнёт врать в день,
// когда пул вырастет или усохнет. Не ответило — обходимся без цифры.
//
// ЧЕГО ЗДЕСЬ НАМЕРЕННО НЕТ. Ни слова про «задачу дня» и про рейтинг: на
// 18.08 задача дня крутит 365 позиций-пустышек, а публичный рейтинг состоит
// из выдуманных игроков (см. ЗАПУСК-2026-08-30.md, Б-1 и Б-2). Обещать их до
// выкатки починки — ровно то, из-за чего страницы платформы уже теряли доверие.

export const metadata: Metadata = {
  title: "CyberChess — запуск 30 августа",
  description:
    "Шахматы с ИИ-коучем, полмиллиона задач и турнирами. Оставьте адрес — напишем в день запуска и пришлём условия раннего доступа.",
  openGraph: {
    title: "CyberChess — запуск 30 августа",
    description:
      "ИИ-коуч, полмиллиона задач, турниры, античит. Ранний доступ по адресу почты.",
    // Контент посадочных русский, а корневой layout объявляет lang="en":
    // проверено запросом от имени поискового робота — в серверной разметке
    // 2167 кириллических символов при lang="en" и без hreflang. Для
    // поисковика и превью в мессенджерах это рассогласование, и оно решается
    // здесь точечно: трогать общий layout нельзя, остальной сайт двуязычный.
    locale: "ru_RU",
    type: "website",
  },
};

const PAPER = "#f7f6f2";
const INK = "#16161a";
const MUTED = "#5d5f66";
const GOLD = "#a9781a";

/** Размер банка задач — из живого API. null, если ответа нет: цифру без подтверждения не показываем. */
async function fetchPuzzleBank(): Promise<number | null> {
  try {
    const r = await fetch(`${getApiBase()}/api/cyberchess-puzzles/meta`, { next: { revalidate: 3600 } });
    if (!r.ok) return null;
    const j = (await r.json()) as { bankTotal?: number; poolSize?: number };
    const n = j.bankTotal ?? j.poolSize ?? 0;
    return n > 0 ? n : null;
  } catch {
    return null;
  }
}

/** Сколько турниров объявлено. Тем же правилом: нет ответа — нет числа. */
async function fetchTournamentCount(): Promise<number | null> {
  try {
    const r = await fetch(`${getApiBase()}/api/cyberchess-tournaments/list`, { next: { revalidate: 3600 } });
    if (!r.ok) return null;
    const j = (await r.json()) as { count?: number; tournaments?: unknown[] };
    const n = j.count ?? (Array.isArray(j.tournaments) ? j.tournaments.length : 0);
    return n > 0 ? n : null;
  } catch {
    return null;
  }
}

export default async function CyberChessLaunchPage({
  searchParams,
}: {
  searchParams: Promise<{ c?: string | string[] }>;
}) {
  const [bank, tournaments] = await Promise.all([fetchPuzzleBank(), fetchTournamentCount()]);
  const left = daysUntilLaunch(Date.UTC(2026, 7, 30)); // 30 августа 2026

  // Метка канала из адреса: /cyberchess/launch?c=tt в подписи ролика TikTok,
  // ?c=ig в шапке Instagram. Без неё все адреса лягут с одинаковым
  // source="cyberchess", и после запуска на вопрос «какой ролик привёл людей»
  // ответа не будет вовсе — а именно он решает, куда вкладывать следующий.
  //
  // Через channelFrom, а не напрямую: неизвестное значение превращается в null
  // и метки не будет. Иначе первый же чужой параметр в ссылке заведёт в
  // выгрузке подписчиков мусорный канал, который потом не отличить от нашего.
  // Внутренние переходы — только через keepChannel. Подставлять сюда `channel`
  // нельзя: он уже НОРМАЛИЗОВАН (youtube), а страница ждёт короткий ключ (yt),
  // и channelFrom("youtube") вернёт null. Ссылка выглядела бы несущей метку и
  // молча её теряла — так и было здесь до 29.08.2026.
  const channel = channelFrom((await searchParams).c);
  const source = channel ? `cyberchess-${channel}` : "cyberchess";
  const bankLabel = bank ? new Intl.NumberFormat("ru-RU").format(bank) : null;

  return (
    // lang="ru" на самом блоке содержимого — потому что <html lang> задаётся в
    // общем шаблоне сайта и стоит "en" (большая часть страниц английская).
    // Замер 28.08.2026 по отдаваемому HTML: здесь 951 русская буква против 28
    // латинских, а страница объявлена английской. Правка в браузере это чинит,
    // но робот, который не исполняет скрипты, видит английскую пометку.
    // Пометка на элементе — стандартная и действует для вложенного текста:
    // ближайший lang выигрывает у корневого.
    <main lang="ru" style={{ minHeight: "100vh", background: PAPER, color: INK, padding: "32px 18px 56px" }}>
      <div style={{ maxWidth: 560, margin: "0 auto", display: "flex", flexDirection: "column", gap: 28 }}>
        <header>
          <div style={{ fontFamily: "monospace", fontSize: 12, letterSpacing: "0.12em", color: GOLD, textTransform: "uppercase" }}>
            AEVION · CyberChess
          </div>
          <h1
            style={{
              fontFamily: "Georgia, 'Times New Roman', serif",
              fontSize: 34,
              lineHeight: 1.15,
              margin: "10px 0 0",
              letterSpacing: "-0.01em",
            }}
          >
            Открываем 30 августа
          </h1>
          <p style={{ color: MUTED, fontSize: 15.5, lineHeight: 1.6, margin: "12px 0 0" }}>
            {left > 0
              ? `Через ${left} ${left === 1 ? "день" : left < 5 ? "дня" : "дней"}. Оставьте адрес — напишем в день запуска и пришлём условия раннего доступа, пока цена стартовая.`
              : "Уже открыто. Оставьте адрес, если хотите получать разборы и новости о турнирах."}
          </p>
        </header>

        <LandingView source={source} />


        <WaitlistCapture
          source={source}
          tone="light"
          title="Написать вам в день запуска"
          description="Одно письмо на запуск и условия раннего доступа. Ничего больше."
          // Подтверждение называет ДАТУ, как и всё остальное на этой странице.
          // Общий текст компонента — «напишем, когда будет что показать» —
          // верен там, где даты нет, но здесь он звучал расплывчатее самого
          // обещания, и человек уходил с меньшей уверенностью, чем пришёл.
          doneText="Готово — адрес записан. Напишем 30 августа, в день открытия."
        />

        {/* Приложение УЖЕ открыто, и это надо говорить рядом с формой, а не
            только внизу страницы. Замер 21.08 на телефоне: единственная ссылка
            «зайти и попробовать» лежала на y=1244 — полтора экрана вниз.
            Человек, готовый попробовать прямо сейчас, до неё не доходил. */}
        <p style={{ color: MUTED, fontSize: 13.5, lineHeight: 1.6, margin: 0 }}>
          {/* В ДЕНЬ запуска «не хотите ждать» становится ложью: ждать уже
              нечего. Оба текста ниже развилкой по left, чтобы 30 августа
              страница звала играть, а не объясняла, что можно не ждать.
              Адрес строится через keepChannel — помощник соседней ветки,
              который сохраняет метку канала на внутренних переходах; без
              него /cyberchess приходит в отчёт как «источник неизвестен».
              При сведении 29.08 взяты ОБЕ стороны: их помощник и моя
              развилка по дню запуска. */}
          {left > 0 ? <>Не хотите ждать?{" "}</> : <>Открыто.{" "}</>}
          <a href={keepChannel("/cyberchess", channel)} style={{ color: GOLD, fontWeight: 600 }}>
            {left > 0 ? "приложение уже открыто — попробовать сейчас" : "играть прямо сейчас"}
          </a>
          .
        </p>

        <section style={{ display: "flex", flexDirection: "column", gap: 14 }}>
          <h2 style={{ fontFamily: "Georgia, serif", fontSize: 21, margin: 0 }}>Что уже работает</h2>

          <div style={{ display: "flex", flexDirection: "column", gap: 10 }}>
            {bankLabel ? (
              <Fact title={`${bankLabel} задач в банке`} note="С темами и рейтингом, а не набор картинок. Число берётся из базы при сборке страницы." />
            ) : null}
            <Fact
              title="ИИ-коуч, который учит по-разному"
              note="Разбор партии со стороны движка: где ошибка, почему она ошибка и что делать вместо."
            />
            {/*
              ЗАДАЧА ДНЯ ВЕРНУЛАСЬ В СПИСОК (28.08.2026).
              Выше в шапке файла записано, почему её здесь не было: на 18.08 она
              крутила 365 позиций-пустышек. Перемерил сегодня на живом проде —
              условие снято: ручка отдаёт настоящую позицию из банка
              (`source: "ChessPuzzle — настоящий банк задач"`, poolSize 502 584),
              и в приложении есть экран «Задача дня» с серией.
              Про ТАБЛИЦУ ЛИДЕРОВ по-прежнему ни слова: она пуста (проверено,
              `items: []`), и обещать её сегодня было бы тем же самым, от чего
              страницу и чистили.
            */}
            <Fact
              title="Задача дня из настоящего банка"
              note="Каждый день новая позиция и своя серия решений. Не набор картинок: задачи те же, что в банке на полмиллиона."
            />
            {/*
              ЧИСЛО ТУРНИРОВ УБРАНО НАМЕРЕННО (28.08.2026).
              Здесь стояло «12 турниров с сеткой и рейтингом» — счёт из
              `/api/cyberchess-tournaments/list`. Двенадцать записей там правда
              есть, но это ЗАГОТОВКИ (`buildSeedFixtures`), и число участников у
              них вписано: 87, 64, 211 из 256. Настоящих регистраций во всех
              двенадцати — НОЛЬ (проверено запросом: `registeredUserIds` пуст
              везде).
              Человек читает «12 турниров», заходит и видит турнир на 211
              участников, которых нет. Обещание сильнее продукта — это первый
              пункт ворот запуска.
              Сама возможность настоящая: сетка, круги, регистрация и таблица
              работают. Поэтому убрано ЧИСЛО, а не пункт: количество здесь
              означало бы активность, а её пока нет.
              Числа участников в заготовках — отдельная задача, она трогает
              семнадцать заготовок и отрисовку сетки, и делать её за два дня до
              запуска дороже, чем убрать одно слово. Вынесено основателю.
            */}
            {tournaments ? (
              <Fact title="Турниры с сеткой и рейтингом" note="Регистрация, круги, таблица результатов." />
            ) : null}
            <Fact
              title="Игра с людьми и с движком"
              note="Подбор соперника по времени и рейтингу; ходы проверяет сервер, а не браузер."
            />
            <Fact
              title="Античит на стороне сервера"
              note="Подозрительные партии помечаются автоматически, без ручного разбора каждой."
            />
          </div>

          <p style={{ color: MUTED, fontSize: 13.5, lineHeight: 1.6, margin: 0 }}>
            {left > 0 ? "Пока идёт подготовка, приложение уже открыто:" : "Играть можно прямо сейчас:"}{" "}
            {/* Метку канала несём ДАЛЬШЕ: без неё цепочка измерения рвётся на
                последнем шаге — мы знаем, какой ролик привёл человека на
                посадочную, но не знаем, кто из них дошёл до самой игры. */}
            <a href={keepChannel("/cyberchess", channel)} style={{ color: GOLD, fontWeight: 600 }}>
              зайти и попробовать
            </a>
            .
          </p>
        </section>

        <footer style={{ borderTop: `1px solid rgba(22,22,26,0.12)`, paddingTop: 16 }}>
          <p style={{ color: MUTED, fontSize: 13, lineHeight: 1.6, margin: 0 }}>
            Мы пишем только по делу и только про шахматы. Отписка — одной ссылкой в каждом письме.
          </p>
        </footer>
      </div>
    </main>
  );
}

function Fact({ title, note }: { title: string; note: string }) {
  return (
    <div
      style={{
        background: "#fffdf8",
        border: "1px solid rgba(22,22,26,0.10)",
        borderRadius: 12,
        padding: "14px 16px",
      }}
    >
      {/* Заходы сюда не считались до 28.08.2026, а запуск 30-го. Без этого
          числа нельзя ответить на вопрос первого дня: продаж нет из-за
          страницы или из-за отсутствия трафика. Компонент сам читает ?c=
          из ссылки, поэтому канал тоже попадёт в замер.
          Возвращено при сведении 28.08: строка потерялась в конфликте с
          правкой вкладки запуска, у которой учёта не было. */}
      <PageTracking page="cyberchess-launch" />
      <div style={{ fontFamily: "Georgia, serif", fontSize: 16.5, fontWeight: 700 }}>{title}</div>
      <div style={{ color: MUTED, fontSize: 13.5, lineHeight: 1.55, marginTop: 4 }}>{note}</div>
    </div>
  );
}
