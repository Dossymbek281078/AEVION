# Handoff — 2026-05-04 (ночь 03→04 мая, переезд на другой ноут)

> Пишет Opus 4.7. Окно: `aevion-core` (главный воркtree, ветка ушла в `ci/i18n-parity-gate`).
> Сессия закрыта по запросу пользователя — «ухожу спать, перенеси на другой ноутбук».

---

## 1. Что доехало до main за смену

| PR  | Тема | Коммит на main |
|-----|------|----------------|
| #80  | fix(security): H4 tokenVersion + M5 OAuth sid + M6 cookie append | `8d4910d` |
| #85  | polish: mobile-reflow grids on bureau/qright + curl quick-start в /developers | `3688e81` |
| #106 | i18n(kk): закрыли 72-key parity gap в `i18n-data.ts` + audit script | `2da2f06` |

Контекст: PR #95 закрыт без мерджа — он был против старого монолитного `i18n.tsx` до того, как параллельная сессия сплитнула словарь в `i18n-data.ts` через PR #91. Перевыложил как #106 на новой структуре.

PR #100 (fix(healthai) — dynamic require + String() params) запушен параллельной сессией пока я писал свой эквивалент. Мою копию выкинул, конфликт прошёл через `git reset --hard origin/main`.

---

## 2. Что висит — ОТКРЫТЫЙ PR

**PR #110 — ci: gate frontend job on i18n en/ru/kk parity**
- Ветка: `ci/i18n-parity-gate`
- Файл: `.github/workflows/ci.yml` — добавил шаг `i18n parity (en/ru/kk)` в Frontend job
- Вызывает `scripts/i18n-kk-extract.mjs` из корня репо, exit 1 если en-only ключ без ru/kk
- Сейчас parity = 102/102/102, гейт должен пройти

**Что сделать завтра первым делом:**
```bash
cd /c/Users/user/aevion-core
git fetch --all --prune
"/c/Program Files/GitHub CLI/gh.exe" pr checks 110
# если зелено — мерджим:
"/c/Program Files/GitHub CLI/gh.exe" pr merge 110 --squash --delete-branch
```

---

## 3. Локальное состояние (этот ноут)

- Главный worktree `C:/Users/user/aevion-core` — на ветке `ci/i18n-parity-gate`, она запушена.
- Working tree чистый кроме `?? frontend-bank/ frontend-chess/ ...` (это другие worktrees, не трогаем).
- 13 stash entries в `git stash list` — все из других сессий, не моё. Не чистить без подтверждения.

Все мои изменения запушены на `origin`. На новом ноуте:
```bash
git clone https://github.com/Dossymbek281078/AEVION.git aevion-core
cd aevion-core
git fetch --all
git checkout main
git pull
```

После этого ветка `ci/i18n-parity-gate` доступна как `origin/ci/i18n-parity-gate` если PR #110 ещё не смерджен.

---

## 4. Что дальше (master plan §5 для `aevion-core`)

После мерджа #110:

1. **P0-1+P0-2 — DNS + SSO cutover.** Главный анблокер всего prod-ready пути. Vercel `*.vercel.app` всё ещё SSO-gated (per memory `reference_prod_deploy_state_2026-05-03`), пока нет публичного домена — никто из инвесторов и краулеров наши surface'ы не видит. Это инфра-работа, мне самому не сделать — нужен пользователь.

2. **P3-1 — Demo recording script.** Файл `docs/AEVION_DEMO_SCRIPT.md` уже есть, но не проверял насколько актуален. Нужно сверить с текущим состоянием Bank / QRight / Bureau / Awards и обновить шаги.

3. **i18n parity для EXTRA блоков.** Главные `translations` теперь в parity 102/102/102. Но в `i18n-data.ts` есть ещё 8 EXTRA-словарей (BANK 2956 ключей × 3 языка, AWARDS, HELP, AWARDS_PORTAL, AWARDS_TRACK_PANEL, PLANET_ARTIFACT, AWARDS_HUB, MODULE_PAGE). По моему скрипту они все 0 missing, но скрипт по умолчанию считает только главный блок. Если хочется полной защиты — расширить `scripts/i18n-kk-extract.mjs` чтобы тоже проверял EXTRA блоки.

4. **Smoke-тесты для security pass (PR #80) поведенческие.** Проверить что bumping `AEVIONUser.tokenVersion` действительно делает запросы 401, и что OAuth-токен теперь имеет `sid` и попадает в `/sessions`. Файл для этого — `scripts/aev-smoke.mjs` или новый `scripts/auth-smoke.mjs`.

---

## 5. Параллельные окна — что не трогать

- `frontend-bank/` — bank track в `aevion-bank` worktree
- `frontend-chess/` — CyberChess (drag/premove ещё сломан, см. memory `cyberchess_other_laptop_pending`, нужен другой ноут)
- `frontend-qcore/` — QCoreAI на ветке `port-qbuild-v3` (ещё не смерджен)
- `aevion-qsign/` — QSign DONE, не трогать
- Ветки `port-*` — другие сессии, оставить им

---

## 6. Команда для нового ноута завтра

После клона / pull на новом ноуте, открыть это окно (Claude Code в `aevion-core`) и сказать:

> «Продолжаем с того места где остановились вчера ночью. Прочитай `HANDOFF_2026-05-04.md` и продолжи: (1) проверь и смерджи PR #110 если зелёный, (2) дальше идём по списку §4 этого хэндоффа.»

или просто:

> «Продолжаем»

— я прочитаю handoff + память и подберу следующий слот.
