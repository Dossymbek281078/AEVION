node_modules
# Keep environment variables out of version control
.env

# Локальные JSON-стора MVP (QTrade и др.)
.aevion-data/

# Pricing leads + analytics events + newsletter (PII / IP — НЕ коммитим)
data/leads.jsonl
data/events.jsonl
data/newsletter.jsonl

/src/generated/prisma
