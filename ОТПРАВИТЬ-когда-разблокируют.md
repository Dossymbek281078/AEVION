# QVenture — 16 коммитов ждут отправки (GitHub заблокирован)

**Дата:** 2026-07-27 · **Ветка:** `feat/qventure-hardening` · **PR** #937

## Что случилось

GitHub-аккаунт приостановлен — `403, Your account is suspended`. Пуш и мерж стоят.
Последний успешно отправленный коммит — `b4e39c7f`. Всё, что после, лежит локально.

Работа не потеряна: она в git, в рабочем дереве, и продублирована в память Claude
(`project_qventure.md`, `feedback_green_tests_on_synthetic_inputs.md`).

## Что сделать, когда аккаунт вернут

```
cd C:\Users\user\aevion-qventure
git status                                    # должно быть чисто
git push origin feat/qventure-hardening
```

Дальше — дождаться зелёных `Backend (tsc + integration tests)` и `Frontend (next build)`
и смержить PR #937.

## Что проверено локально на 2026-07-27

| Проверка | Результат |
|---|---|
| `npx tsc --noEmit -p tsconfig.json` (бэкенд) | чисто по зоне qventure |
| то же (фронтенд) | 0 ошибок вне `.next` |
| тесты бэкенда | 2153 зелёных |
| корпус раскрытых цифр | 89/89, разрыв 10.9 |
| трудные случаи | 30/30 |

Две оговорки, обе не блокируют:

- **`TS1261 kidsAiContent`** — git хранит `kidsAiContent.ts`, на диске лежит
  `kidsaicontent.ts`. Ошибку видит только Windows с нечувствительной к регистру
  файловой системой; на Linux в CI её нет. Существует и на чистом дереве, старше
  этой ветки.
- **Ошибки внутри `.next/dev/types`** — мусор от старого dev-сервера, лечится
  `rm -rf .next`. Исходников не касаются.

## Что в этих коммитах

- 741ebafa fix(qventure): three phrasings only a real filing would have shown
- 6fd252ac docs(qventure): the duplicate hunt, including the guard that did not guard
- 97f097e5 chore(qventure): keep the duplicate scanner, out of the scratch directory
- 9b4efd68 feat(qventure): first corpus cases whose filings never quoted a dollar
- bf8f03ee refactor(qventure): one reader for a percentage band, not three
- fc52470b feat(qventure): Turkish lira, a seventh new company, a seventh new defect
- ba06eeba feat(qventure): thirteen currencies move from refused to read
- a1859436 fix(qventure): the deck's own notation, for the metric that lacked it
- 1d71c479 fix(qventure): the notations a deck writes instead of a sentence
- 1aad862e refactor(qventure): twenty-four connector lists become one constant
- a786b8d8 fix(qventure): a figure too large to be a currency it did not name
- 3402462c fix(qventure): finish the notation sweep — CAC, LTV and TAM
- 44b1e8cc fix(qventure): the deck cross-check knew fewer scales than the parser
- 9d95cefd fix(qventure): five of twelve fields scored a target as a result
- 874a1ae0 fix(qventure): the deck veto knew fewer nouns than the parser
- ce4b8bc3 refactor(qventure): the deck path declares its fields once

Подробности каждого — в `docs/benchmarks/qventure-rubric.md`, пределы 22–41.

## После мержа

Прогнать живой `/api/qventure/analyze` по всем 30 кейсам корпуса. Прод сейчас на
рубрике v5, всё описанное — v6, и это единственная проверка, которой пока нет:
все замеры сделаны против движка, не против HTTP-маршрута.
