import { defineConfig } from "vitest/config";
import path from "node:path";

export default defineConfig({
  test: {
    environment: "jsdom",
    globals: true,
    include: ["src/**/*.test.ts", "src/**/*.test.tsx"],
    setupFiles: ["./vitest.setup.ts"],
    css: false,
    // Умолчание 5 с не вмещает этот набор. Замерено 12.08.2026: в полном
    // прогоне падали 1-2 СЛУЧАЙНЫХ файла каждый раз, всегда с "Test timed out
    // in 5000ms", а изолированно те же файлы проходили за 2.3 с на всё. Дольше
    // всего идут не сами проверки (23-60 с суммарно), а окружение и setup —
    // jsdom поднимается для каждого файла, и под параллельной нагрузкой
    // отдельный тест доходил до 8.2 с.
    //
    // Важно, чем это НЕ является: не регрессией теста и не нехваткой памяти.
    // Такие падения выглядят как случайная краснота CI на посторонних правках,
    // и списывать их на «нагрузку» — тот самый способ годами держать красный
    // набор. 30 с — значение, которым стабилизировали бэкендовый набор.
    testTimeout: 30_000,
    hookTimeout: 30_000,
    // Тот же предел, что и у бэкенда, но по более скромным основаниям — говорю
    // как есть. Ядер 32, vitest по умолчанию берёт почти все, а на машине живут
    // несколько сессий. Замер 10.08.2026: по умолчанию 27 с, с пределом 21 с,
    // оба прогона зелёные — то есть выигрыш во времени небольшой, и сам по себе
    // он правки не стоил бы.
    //
    // Стоит её другое: за этот день дважды на ОДИНОЧНОМ файле прогон падал с
    // «[vitest-pool-runner]: Timeout waiting for worker to respond» — воркер не
    // успевал ответить за 60 с, хотя тесты в файле идут доли секунды. Это то же
    // голодание, что на бэкенде превращало 2-секундные файлы в таймауты. Предел
    // делает такие срывы менее вероятными.
    maxWorkers: 4,
  },
  resolve: {
    alias: {
      "@": path.resolve(__dirname, "src"),
    },
  },
});
