# Денежный путь: от кнопки до кабинета

Ветка `fix/ls-devhub-subscription-entitlement`. Одиннадцать коммитов, все
про одно: человек платит — и получает ровно то, что купил, а мы это видим.

## Что чинилось (каждое — измерено на живом проде, не по коду)

| Дефект | Как проявлялся |
|---|---|
| Подписка DevHub $149/мес выдавала тариф «lite» ($19) | Товара не было в таблице соответствия → откат в дефолт. Ответ 200, магазин не повторял доставку, следа нет |
| Отмена подписки DevHub не забирала доступ | Строка помечалась `cancelled`, тариф оставался `pro` навсегда |
| Неизвестный товар превращался в тариф наугад | «Безопасный дефолт» на денежном пути = молчаливая выдача не того |
| После оплаты — 404 | Провайдер вёл на `/payment/success`, страницы с таким адресом нет ни в коде, ни на проде |
| Экран успеха называл Gumroad всем | Включая плативших через Lemon Squeezy и PayPal — их отправляли искать подписку в чужой аккаунт |
| Купленный модуль не учитывался проверкой доступа | `planGate` брал права только из тарифа; при включении пейволла купивший упёрся бы в отказ |
| Подписчик QPayNet упёрся бы в отказ | Покупка `qpaynet`, модуль `qpaynet-embedded` — сопоставления не было |
| Кабинет говорил «нет подписки» платящему | Читал только тариф, поштучные покупки не показывал |
| Замер стоял на одной странице из шести | Ролики ведут на `/qrenew` и `/qmelanin` — там не считалось ничего |
| Не видно, уходят ли письма покупателю | Без ключа отправка «успешна» и молча в лог |

Плюс отчёт `scripts/sold-vs-enforced-report.mjs`: сверяет, что продаётся, с
тем, что реально закрыто платным доступом. Сегодня расхождение — 8 из 9
продаваемых модулей открыты всем, а 6 закрытых нельзя купить отдельно. Это
отчёт, а не сторож: код выхода всегда 0, потому что решение продуктовое.

## Проверки

- На каждой правке — **мутация**: возвращаю дефект, тесты падают; возвращаю
  правку, проходят. В сообщениях коммитов указано, сколько именно падает.
- `tsc` обеих половин чист, `next build` — Compiled successfully.
- Полные наборы: **бэкенд 1445 тестов, фронт 560, ноль падений.**

## Для того, кто будет мержить

Ветка сливается с `main` **чисто**. С четырьмя ветками из очереди — конфликты,
и все они в файлах, которые я правил. Проверено контролем: ещё шесть веток
«конфликтовали» только потому, что сами разошлись с `main`; их я не задеваю.

| Ветка | Конфликтующие файлы |
|---|---|
| `feat/pitch-monetization-funnel` | `routes/events.ts`, `app/apps/page.tsx`, `app/go/page.tsx`, `app/pricing/admin/page.tsx` |
| `integration/auth-key-reconciled` | те же четыре |
| `fix/pitch-one-denominator` | `app/go/page.tsx` |
| `test/paywall-provision-flow` | `tests/paywallProvisionFlow.test.ts` (add/add) |

**Все мои правки в этих файлах — добавления, не переписывание:**

- в `events.ts` — два разреза сводки (`byChannel`, `byProduct`);
- в страницах — одна строка `<PageTracking page="…" />`;
- в `pricing/admin/page.tsx` — два блока вывода этих разрезов;
- в `paywallProvisionFlow.test.ts` — только `await` у вызовов гейта
  (он стал асинхронным) и комментарий; **ни одно утверждение не тронуто**,
  это видно по дифу.

При разрешении конфликта берите обе стороны. Единственное, что нельзя
потерять: `await` перед `requireModule(...)` — без него тест читает состояние
раньше, чем гейт успел ответить, и молча зеленеет на сломанном коде.

## Что осталось за основателем

1. Цены: сайт показывает $24/$39/$89, магазин списывает $19/$29/$49.
2. Что продаём и что закрываем — три варианта в
   `Desktop\АЕВИОН\00-НАЧНИ-ОТСЮДА\2026-08-13-ЧТО-ЖДЁТ-ВАШЕГО-СЛОВА.md`.
3. `EVENTS_FILE` в Railway — иначе первая выкатка сотрёт накопленный замер.
4. После выкатки проверить `emailSender` в `/api/health`.
