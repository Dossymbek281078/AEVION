# Доска запуска AEVION · старт 30 августа 2026

> Правит **только окно запуска** (`aevion-launch`). Остальные окна отвечают
> результатом в своих ветках — доску не редактируют.
> Состояния: `не начато` · `в работе` · `на приёмке` · `принято` · `отложено`.
> Заведена 16.08.2026.

---

## Состояние на 19.08.2026 — коротко, чтобы не читать 500 строк

**До старта 11 дней.**

| Модуль | Дата | Страница | Посадочная | Приём адресов | Цена |
|---|---|---|---|---|---|
| CyberChess | 30.08 | да | да | да | $19 |
| QRight | 06.09 | да | пара с Бюро | **да** (с 19.08) | — |
| IP Bureau | 06.09 | да | да | да | $29 |
| DevHub | 13.09 | да | да | да | $149 |
| Multichat | 20.09 | да | да | да | — |

**Ворота, пройденные фактом:** оплата доходит до кассы (2), отказ показывается
отказом (4), возврат на второй день (5), письмо соответствует подписке (6),
сторож знает модуль (7). **Не пройдены:** путь новичка целиком (3) — первый
экран шахмат с тремя накладками, причина найдена и передана.

**Здоровье платформы (проверяется само):** враждебный ввод 5 OK / 0 FAIL ·
страницы 46/46 · телефон 12/12.

**Ждёт основателя:** `Desktop\АЕВИОН\00-НАЧНИ-ОТСЮДА\2026-08-19-ЧТО-ЖДЁТ-ВАШЕГО-СЛОВА.md`

Ниже — журнал по дням, самое свежее в конце.

---

## Правило приёмки

Задача считается сделанной, когда **окно запуска само** увидело результат на
проде или на превью и записало сюда замер. Отчёт исполняющего окна —
не доказательство. Формулировка «улучшить», «доработать», «причесать» в
задачу не попадает: если нельзя проверить предложением, задача не готова к
выдаче.

---

## 1. CyberChess — 30 августа

Движок готов и это подтверждено замером 16.08: 500 000 задач в базе
(`bankTotal` 502 584), 22 темы, 12 турниров, античит, голосовой коуч,
матчмейкинг. Цена $19 в каталоге есть, платёжная ссылка жива.

Блокеры ниже найдены проверкой «обещает = делает» и до старта закрываются.

### 🔴 Б-0. Работа окна шахмат не доехала до прода — это и есть корень Б-1 и Б-2

**Замер 16.08.** На проде живёт версия `cyberchessDaily.ts`, в которой нет
**восьми с лишним коммитов и 545 строк** работы окна шахмат: персистентность
рекордов в Postgres, защита от затирания двумя репликами, разбор таймзон,
диагностика без утечки устройства инфраструктуры, честный отказ при
недоступной базе. Всё это написано, лежит в ветках `feat/cyberchess-human-weak-bots`
и `deploy/chess-only` — и не выкатано.

Из этого же корня растут обе находки ниже: **их починки уже существуют**, и
единственное, чего им не хватает, — выкатки.

**Что делать:** окно шахмат довозит свою работу до прода само — 545 строк
чужой активной зоны не мержатся со стороны. Базу брать по `/health`, а не
свою ветку, иначе выкатка снесёт чужое (так 14.08 откатилась правка письма).

**Критерий приёмки:** `/health` называет коммит, содержащий `f67d9093d`;
проверки Б-1 и Б-2 ниже проходят на живом проде.

**Окно:** шахматы · **состояние:** не начато · **срок:** до 23.08

### 🟠 Б-1. «Задача дня»: на проде 365 позиций-пустышек, в ветке — честные 30

**Замер 16.08:** прод отдаёт `poolSize: 365`, потому что там
`POOL = [...HAND_CRAFTED, ...generateProcedural(335)]`, а `generateProcedural`
берёт позицию из `TEMPLATE_BASES` по кругу (`i % 10`) и меняет **только тему и
рейтинг**. Это 10 позиций по 33 раза, и все десять — дебютные, а не тактика;
тема «Sacrifice · 1650» присвоена случайным генератором и к позиции отношения
не имеет.

**В ветках шахмат это уже исправлено:** `POOL = HAND_CRAFTED` — 30 задач,
сделанных руками, и ответ честно помечает, что это резервный пул. Паллиатив,
но правдивый.

**Остаётся после выкатки:** 30 задач дают цикл повтора в месяц, а настоящий
пул на 500 000 записей с реальными темами и рейтингами уже лежит в Postgres
(`db ChessPuzzle`, `bankTotal` 502 584) и отдаётся ручкой
`/api/cyberchess-puzzles`. Задача дня должна брать оттуда, детерминированно по
дате.

**Критерий приёмки:** три разных дня — три разные позиции; тема и рейтинг
совпадают с записью в `ChessPuzzle`; один и тот же день даёт одну и ту же
задачу.

**Окно:** шахматы · **состояние:** починка в ветке, ждёт выкатки; переход на
пул из БД — не начат

### 🟠 Б-2. Публичный рейтинг: на проде 100 выдуманных игроков, в ветке — уже убраны

**Замер 16.08:** `/api/cyberchess-daily/leaderboard` отдаёт 100 записей
`seed_000`…`seed_099` с именами реальных гроссмейстеров (Magnus, Hikaru,
Fabiano, Ding) и стриками 344–365 дней. Живых игроков ноль.

**В ветке шахмат это убрано 10.08**, и формулировка их комментария совпадает с
выводом дословно: «таблица, показывающая незнакомцев впереди реального игрока,
только что занявшего первое место, хуже пустой». Старые seed-строки
отбрасываются при загрузке (`isSeededEntry`).

**Критерий приёмки:** на проде в таблице нет ни одной строки, которую нельзя
объяснить пришедшему игроку.

**Окно:** шахматы · **состояние:** починка в ветке, ждёт выкатки

### 🟡 Б-3. Путь новичка — пройден наполовину 18.08

**Что проверено на живом проде, браузером, без аккаунта:**

Партия с ИИ работает целиком. Доска открывается, соперник — Club AI 1200,
движок Stockfish 18, коуч уровня «супер-GM» подключён, часы идут, поражение по
времени засчитано, CPI начислен (+63), после партии предложены разбор, реванш,
новая и QR.

**Три замечания, найденных глазами — ни одно не ловится сторожами:**

1. **Новичок видит три накладки разом.** На первом экране: модалка «Ежедневный
   бонус», плашка «Установи CyberChess как приложение» и всплывашка «Включить
   стрим любимого стримера». Панель «Проекты Аевион» справа закрывает часть
   интерфейса. Для страницы, на которую поведёт платный трафик, это первое
   впечатление.
2. **Плашка установки не уходит в партии** и закрывает левую панель с оценкой
   и материалом — прямо во время игры.
3. **Доска не размечена для доступности:** дерево доступности видит один образ
   «Шахматная доска» без клеток и фигур. Клавиатурой и экранным диктором
   партия недоступна.

**Что осталось непройденным:** пазлы, турнир, покупка $19, тот же путь с
аккаунтом. Отдельной строкой — проверить, что отказ показывается отказом.

**Окно:** запуск · **состояние:** половина пройдена, продолжение за мной

### 🟡 Б-4. Оплата $19 доходит до кассы и открывает купленное

Ссылка жива (200), но путь «нажал → оплатил → получил доступ» на проде не
проходили. По HANDOFF 20.07 у Pro/Ultimate был `PLATFORM_WALLET_ID=null`.

**Критерий:** тестовая покупка проходит целиком, купленное открывается,
вебхук записал продажу.

**Окно:** монетизация · **состояние:** не начато

### 🟢 Б-6. Посадочная страница запуска с приёмом адресов

`/cyberchess/launch` — газетная страница под ролики: обратный отсчёт до 30.08,
форма с `source="cyberchess"`, факты с числами из живого API (502 584 задачи,
12 турниров). Обещаний про задачу дня и рейтинг на ней намеренно нет, пока
Б-1 и Б-2 не выкачены.

Проверено на превью 18.08: страница 200, число из API отрисовано, форма
отправлена вживую — «Готово, адрес записан». **Принято 18.08.**

Мелочь на потом: в правом верхнем углу переключатель языка рисуется дважды
(«RU RU»). Не блокер, но на посадочной под трафик заметно.

### 🟢 Б-5. Модуль в стороже страниц

`/cyberchess` уже в списке `pages-live-smoke.js`; сторож `AEVION-PagesGuard`
идёт каждые 30 минут. **Принято 16.08** (39/39 PASS).

---

## 2. QRight + IP Bureau — «патентное бюро» — 6 сентября

Готовится параллельно, своим окном. Цена $29 в каталоге есть.

- **К-1.** Пройти ворота раздела 3 CLAUDE.md целиком. · не начато
- **К-2.** Контур QRight → QSign → Bureau сквозняком. · **принято 18.08:**
  реестр отвечает (`/api/qright/objects` 200); подпись работает по-настоящему
  (`sign` → `verify` = `valid:true`, испорченная подпись → `false`);
  QSign v2 жив — `qsign-v2.0`, канонизация RFC 8785, база ok, 42 подписи;
  публичная проверка сертификата без входа отвечает 200.
- **К-3.** Лист ожидания Notarized: с 11.08 форма пишет на сервер (раньше
  адрес оставался в браузере). Проверить на проде. · не начато
- **К-4.** Посадочная `/bureau/launch` с приёмом адресов и меткой канала.
  · **сделано 18.08**, ждёт проверки на проде после выкатки.
- **К-5.** Числа про масштаб на страницах бюро — **нельзя**: в реестре 25
  объектов, без пробных пять («1», «Музыка 1», «My First Track»);
  сертификатов пять, три из них smoke test. Обещаем работу, не размер.

## 3. DevHub Studio — 13 сентября

- **Д-1.** Честный прогон сборки: деплой помечается живым только после того,
  как задеплоенный адрес реально отдал 2xx. · не начато
- **Д-2.** Цена $149 — самый дорогой чек; путь оплаты пройти первым. · не начато

## 4. Multichat / QCoreAI — 20 сентября

- **М-1.** Цена не назначена — решение основателя. · ждёт решения
- **М-2.** Консилиум агентов как демонстрация: сценарий на 60 секунд. · не начато

---

## Общее для всех запусков

| Что | Состояние |
|---|---|
| приём адресов на главной и `/go` | принято 14.08, пишет в Postgres |
| письмо подписчику по источнику | принято 14.08, тема зависит от страницы |
| потолок писем Brevo — 300 в сутки | **ждёт решения основателя** (тариф) |
| цены у 34 модулей из 41 | **ждёт решения основателя** |
| ссылки на закрытый GitHub на 21 странице | **ждёт решения** (публичного зеркала нет) |
| ML-DSA-65 заявлен на `/investor`, на проде нет | фикс готов в чужой ветке, не влит |

---

---

## Замер 18.08: невыкаченная работа по всем модулям очереди

До старта 12 дней. Прод-бэкенд — `bb9033993df5` (`deploy/combined-2026-08-14`,
собран 16.08). Считал коммиты, которых в нём нет, по путям каждого модуля —
отдельно по всем веткам и отдельно по 54 веткам, тронутым за последнюю неделю.
Вторая колонка честнее: в первую попадают брошенные и экспериментальные ветки.

| Модуль | не на проде, все ветки | из них в живых ветках |
|---|---:|---:|
| CyberChess | 340 | **190** |
| DevHub | 139 | **61** |
| Multichat | 85 | 22 |
| QCoreAI | 152 | 10 |
| IP Bureau | 51 | 8 |
| QRight | 57 | 6 |

**Вывод, который меняет план запуска.** Ни один модуль очереди не упирается в
недописанный код — все шесть упираются в доставку. У шахмат на проде нет 190
свежих коммитов собственной команды, у DevHub — 61. Мы готовим к публикации
версии, которые на две-три недели старше того, что уже написано.

Поэтому до 30 августа главная работа окна запуска — не «доделать», а «довезти
и проверить довезённое». Порядок: шахматы, затем бюро, затем DevHub.

**Оговорка о приборе:** счёт по путям модуля ловит и коммиты, задевшие файл
попутно (общая правка ключа входа, массовая замена текста ошибок). Число —
верхняя граница объёма, а не список полезного. Что именно везти, решает окно
модуля, глядя на свои ветки.

---

## Журнал решений и сдвигов

Сдвиг даты записывается сюда с причиной. Пусто — дат не двигали.

- 16.08 — доска заведена. Первая же проверка шахмат дала два расхождения
  «обещает ≠ делает», и оба оказались следствием одного: работа окна шахмат
  (545 строк, 8+ коммитов) не выкатана на прод. Код написан — не доехал.
- 18.08 — замер невыкаченной работы по шести модулям: у всех есть готовое,
  но не выкаченное. Приоритет запуска смещён с доработки на доставку.
- 19.08 — доставка сорвалась не нехваткой кода, а гонкой выкаток. За час прод
  сменился трижды (`bfc1d33` → `a011c30` → `bd0ec0a`), и каждая выкатка
  снимала чужую. Замер пробником по живому проду: **1 OK / 4 FAIL** — вернулись
  четыре дефекта враждебного ввода, включая денежный (`/api/pricing/quote`
  отдаёт смету без сумм на чужой валюте). Все четыре починки существовали и
  были написаны раньше — их просто откатило.
  **Сделано:** собрана `deploy/launch-united-2026-08-19` = свежий прод +
  бэкенд QSkyway + четвёртая починка (`skill-tests`, взята из
  `deploy/frontend-all-2026-08-18`, а не написана заново). Ноль конфликтов,
  `tsc` = 0 ошибок при мутационно проверенном компиляторе, в зеркале.
  **Не выкачено намеренно:** окно шахмат в этот момент вело цикл выкаток, и
  вмешательство вернуло бы ему его же откат. Задания переданы обоим окнам
  (`aevion-chess-humanbot`, `aevion-qskyway`).
  **Найдено попутно:** `aevion-deploy-check.mjs` судит по ИМЕНИ ветки на проде,
  а не по тому, содержит ли твоя ветка его коммит. Ветка, включающая прод
  целиком, получает «НЕ выкатывайте поверх» — совет верный по осторожности,
  но по неверному признаку.
- 19.08, приёмка шахмат браузером по живому проду (ворота 2 и 3).
  **Игра работает по-настоящему:** «Играть» → `e2-e4` → ИИ ответил `Кf6`, часы
  пошли, дебют определился сам («B02 Alekhine's Defense»), движок
  `Stockfish 18 · d22`. Это главное, и это подтверждено рукой, а не отчётом.
  **Не пройдено:** первая загрузка ~20 секунд с надписью «Подгружаю шахматы…»,
  и в момент старта партии вкладка десятки секунд не отвечала (все запросы при
  этом 200 — значит не сеть, а разбор 400 задач и движок). Для запуска это
  дороже косметики: с телефона такой экран чаще всего закрывают.
  **Четыре замечания по первому экрану:** «SF18 · 0 головоломок400» в шапке
  (проверено в DOM — наш текст, не автоперевод); панель «ПРОЕКТЫ АЕВИОН»
  открыта поверх и режет кнопку «ИГРАТЬ»; всплывашка про стрим перекрывает
  единственную кнопку покупки; плашка дебюта рисуется поверх фигур.
  Всё передано окну шахмат с точными местами.
  **Честно о непроверенном:** мобильную ширину проверить не удалось (окно
  браузера не сжалось), пункт остаётся открытым, а не пройденным.
- 19.08, ворота 1 по QRight (запуск 6.09): проверил обещание «confirmed in a
  Bitcoin block». **Механизм работает** — вызвал `opentimestamps` напрямую на
  случайном хеше: штамп 805 байт, приняли 4 календаря. **Но на проде через
  него не прошёл ни один сертификат:** все пять созданы 12–23 апреля, код
  якорения появился 24 апреля, у всех `bitcoinAnchor.status = not_stamped`.
  Обещание не ложно, но говорит о мгновенном результате там, где он приходит
  часами позже и требует шага upgrade. Текст на странице исправлен: теперь
  названы последовательность и время.
  **Осталось на этой странице (не сделано):** (а) весь `/qright` на английском
  при `lang="ru"` — против правила русской прозы, и это первое, что увидит
  русскоязычный посетитель 6 сентября; (б) у всех пяти сертификатов
  `signatureHmacReason: NO_SIGNED_AT`, то есть проверка HMAC-подписи не
  выполняется вовсе — надо понять, старые это записи или дефект проверки;
  (в) в реестре по-прежнему «smoke test» и «Test Patent» — витрину к запуску
  чистить или наполнять настоящими объектами.
- 19.08, ворота 2 по шахматам (оплата доходит до кассы): **пройдено.** Ссылка
  из каталога открывает живую кассу Lemon Squeezy — товар «AEVION CyberChess
  Pro», цена 19,00 $, подпись «billed every month», оплата картой и PayPal.
  **Отдельно про метод:** проверка `curl` дала 404 по всем семи ссылкам, и это
  была ЛОЖНАЯ тревога — касса собирается скриптом, а HTML одинаков и у
  настоящего товара, и у выдуманного (проверил контролем: сам магазин отвечает
  200, а придуманный id даёт ту же страницу с тем же заголовком). Такой вывод
  здесь уже делали и уже опровергали; денежные ссылки проверять только
  браузером.
- 19.08, ворота 5 по шахматам (возврат на второй день): **НЕ пройдены.**
  Задача дня отдаётся из запасного пула на 30 позиций; ручка, на которую
  ссылается сам ответ (`/api/cyberchess-puzzles/daily`), отвечает 404 и
  отсутствует во всех ветках. В банке при этом 502 584 задачи. Повтор раз в
  месяц у единственного механизма возврата. Передано окну шахмат; сводка
  готовности исправлена — раньше она называла это пройденным.
- 19.08, ворота 1 по бюро (6.09), витрина реестра: в публичном списке 25
  объектов, из них **20 — записи прод-смоука** (`ownerName: "smoke-test"`), а
  страница прозрачности объявляла «зарегистрировано 25», то есть завышала
  реестр впятеро. Записи оборвались 22.07 не потому, что их убрали, — 27.07
  отключили GitHub, откуда смоук запускался; доступ вернули 18.08, значит
  поток возобновится к запуску. **Сделано:** фильтр по машинной метке в
  четырёх публичных местах (список, поиск, CSV, все счётчики прозрачности);
  ничего не удалено, админский список и `mine=1` не тронуты, смоук от фильтра
  не краснеет (проверено по его утверждениям).
  **Осталось решением основателя:** пробные объекты живых людей («Test
  Patent», «1», «Музыка 1») — их код не прячет, и к 6 сентября витрину надо
  либо наполнить настоящими записями, либо честно показать пустой.
- 19.08, замечание по Multichat (20.09): `/api/multichat/health` отвечает
  **401 «auth required»**. Health за авторизацией означает, что сторож страниц
  и любая внешняя проверка модуль не видят — «жив ли он» снаружи ответа нет.
- 19.08, безопасность перед запуском: в `all-smokes.js` нашёлся зашитый
  **BUILD_PAYMENT_WEBHOOK_SECRET** (с 19.05, во всех ветках). Этой подписью
  `POST /api/build/webhooks/payment` помечает заказ оплаченным и начисляет
  кэшбэк. **Сканер утечек его не видел:** все его шаблоны опознают ключ по
  формату провайдера, а у наших секретов формата нет. Добавлен шаблон по
  соседству + отсев самоописанных заглушек; проверено контрастом — на ветке
  шахмат сканер краснеет и называет файл, на ветке с убранным секретом
  молчит. Хардкод убран, смоук берёт секрет из окружения и пропускается с
  названной причиной. **За основателем:** сменить сам секрет —
  `Desktop\АЕВИОН\15-Аудиты-и-сводки\2026-08-19-СМЕНИТЬ-секрет-платёжного-вебхука.md`
- 19.08, 12:4x — **выкачено на прод** (`41bcb4e89a88`, ветка
  `deploy/launch-united-2026-08-19`). Четыре починки враждебного ввода довезло
  соседнее окно (`deploy/fixes-onto-prod-2026-08-19`, 12:21) — проверено
  пробником: **5 OK, 0 FAIL** против **1 OK / 4 FAIL** утром. Сверху доехало от
  окна запуска: письмо подписчику по модулям, витрина реестра без смоуков
  (в выдаче 0 из бывших 20, прозрачность 10 вместо 25), убран зашитый секрет
  платёжного вебхука. После выкатки проверено: 5 страниц и 5 ручек — 200.
  **Ловушка инструмента:** `railway-deploy.sh` судит «своё/чужое» по ИМЕНИ
  ветки, поэтому правильно собранная ветка требует `ALLOW_OVERWRITE=1`.
  В `aevion-deploy-check.mjs` это исправлено (сверка по предку), в скрипте —
  нет, он в зоне окна шахмат.
- 19.08, ворота 6 (приём адресов и письмо): **пройдены фактом.** Подписался
  сам с посадочной шахмат (`source: cyberchess-launchcheck`) — форма приняла
  (201, postgres), письмо пришло в ящик за минуту: тема «Вы в списке раннего
  доступа: CyberChess», текст «Открываем по плану 30 августа, напишем в день
  запуска», ссылка на `/cyberchess/launch`. До сегодняшней правки на это же
  действие приходило «вы оставили адрес на главной странице» без слова о
  шахматах.
- 19.08, состояние деплоя: Railway прислал **шесть писем «Deployment crashed»**
  за час (06:17–07:23 UTC = 11:17–12:23 местного) — ровно тот период, когда
  прод менялся каждые 5–10 минут. То есть частые выкатки были не работой по
  плану, а повторными попытками после падений.
  **Сейчас прод устойчив, это замерено, а не предположено:** три замера
  `uptimeSec` подряд — 278 → 324 → 369 при неизменном коммите, то есть процесс
  не перезапускается. Формулировка важна: «прод падает» было бы неправдой,
  правда — «шесть выкаток подряд не поднялись, текущая стоит».
- 19.08, замечание мимо очереди запуска: в логах прода повторяется
  `[ShadowNet] GET /posts/:alias db error: invalid input syntax for type
  bigint: "NaN"`. Проверил ручку живыми запросами и прочитал обработчик —
  на неизвестный алиас она отвечает 200 с пустым списком, и это законно
  (списочная ручка). Значит NaN приходит откуда-то ещё; передаю как есть,
  свою первую гипотезу («ошибка глотается и выдаётся за успех») снимаю —
  она не подтвердилась чтением кода.
- 19.08, **ворота 5 закрыты** — окно шахмат починило задачу дня по переданному
  заданию. Замер тем же запросом, что утром давал запасной пул:
  `source: "ChessPuzzle — настоящий банк задач"`, `poolSize: 502584`,
  задача `li_2AEIG`. Утром было `poolSize: 30` и метка fallback.
  Сводка готовности теперь показывает «задача дня из настоящего банка: да».
  **Замечание, не блокер:** у сегодняшней задачи рейтинг 1931. Для «возврата
  на второй день» у новичка (стартовый рейтинг 800) это тяжело; стоит либо
  подбирать по силе игрока, либо честно называть задачу дня сложной.
- 19.08, первый экран шахмат — **измерено, причина найдена, починка в пути.**
  Три `fixed`-элемента одновременно (ширина окна 1463): панель «Проекты
  АЕВИОН» 240×718 (z 150), всплывашка стрима 340×99 (z **7900**, ложится
  поверх самой панели и закрывает в ней кнопку покупки), плашка установки
  360×230 (z 1000). Кнопка «ИГРАТЬ» кончается на 1294, панель начинается с
  1223 — **71 px кнопки под панелью**.
  **Причина не в коде шахмат:** компенсация `paddingRight: 244` и
  `--cc-banner-reserve` в коде есть, а в живом DOM элемента с этой переменной
  нет вовсе и ни у одного элемента нет `paddingRight ≥ 50`. То есть на проде
  **фронт старее кода**: у платформы два прода, и `railway-deploy.sh` не
  двигает Vercel. Этого я утром не различал и записал наблюдение как дефект
  вёрстки — поправка внесена.
  **Выкатка фронта запущена и не состоялась с первого раза:** скрипт честно
  остановился на проверке типов, потому что во фронте не установлены
  зависимости и `npx tsc` подхватил чужой одноимённый пакет — ровно тот подлог
  прибора, что ловил меня утром на бэкенде. Ставлю зависимости, повторяю.
- 19.08, канал заданий: файл, положенный в чужой worktree, **исчез** — он был
  untracked, и его снесло их же работой с ветками. Задание успело сработать
  (ветку смержили, задачу дня починили), но канал ненадёжен. Задания теперь
  живут вне репозиториев: `C:\Users\user\aevion-задания-от-окна-запуска\`.
- 19.08, ворота 7 (сторож знает модуль): у DevHub и Multichat посадочные уже
  отвечали 200 и собирали адреса, а в списке `pages-live-smoke.js` их не было —
  их падение прошло бы незамеченным. Было 41 адрес, стало 43.
- 19.08, ворота 1 по DevHub (13.09): три отметки «работает» на посадочной
  проверены живыми запросами — `devhub/templates`, `devhub/agent/templates`,
  `devhub/media/video/models` отвечают 200, отрицательный контроль даёт 404.
  Названные модели тоже на месте: veo-3, veo-3-fast, seedance в ручке видео,
  trellis и hunyuan3d — в `devhub/media/3d/models` (сперва не нашёл их в
  видео-ручке и чуть не записал ложную находку). Тарифы шахмат $19/$29/$49
  сходятся с живым `/api/pricing`, кнопка «Купить» ведёт на Lite $19 — тот же
  товар, что открылся в кассе.
- 19.08, 🔴 **числа на странице цен требуют решения основателя.** Замер по
  живому API: «12 000+ зарегистрированных идей» при **10** объектах в реестре,
  «3 200+ сертифицированных артефактов» при **5** сертификатах, «30+ стран»
  при **одной** (Казахстан). Числа приходят из `data/trust.ts` как строковые
  константы. Другая сессия нашла их 10.08 и намеренно не стала править —
  «подставить правдоподобное значит выдумать». Решение верное; изменилось то,
  что теперь есть замер. Положил на стол четыре варианта:
  `Desktop\АЕВИОН\15-Аудиты-и-сводки\2026-08-19-ЦИФРЫ-на-странице-цен-требуют-вашего-решения.md`
- 19.08, ворота 4 (отказ показывается отказом): **пройдены** по форме приёма
  адресов. Шесть видов мусора — пустое поле, «не-почта», «a@b», ключ
  прототипа, адрес на 300 символов, запрос без поля — все дают 400 с внятным
  `validation_failed` и русским «неверный формат email». Утечек устройства
  системы в тексте ошибки нет (проверял по словам postgres, railway, стек,
  пути контейнера).
- 19.08, плашка «Установи CyberChess как приложение» (360×230) висела и на
  посадочной запуска — она подключена в layout, то есть на всех страницах
  модуля. Убрана с `/cyberchess/launch`: туда идёт трафик роликов, и человек
  там ещё не игрок. В самой игре плашка остаётся.
- 19.08, сторож страниц после правки списка: **43 из 43 живы**, прогон
  подтвердил и то, что он берёт свежую версию из зеркала.
- 19.08, **поправка к записи выше про первый экран.** Я объяснил отсутствие
  компенсации тем, что «на проде фронт старее кода». Это неверно, снимаю:
  сайт собран сегодня в 08:08Z из `deploy/frontend-all-2026-08-18`, и
  компенсация в этой ветке есть.
  Настоящая причина точная и в коде: контейнер `.cc-main-row`, который несёт
  `paddingRight: 244`, рендерится при
  `(!setup || tab==="puzzles" || tab==="analysis" || tab==="coach")`, а первый
  экран — это `setup === true, tab === "play"`. Компенсировать панель нечему,
  потому что контейнера на этом экране нет. Панель при этом показывается по
  своему условию `vwPx >= 1100`, которое там истинно.
  Перекрыт ровно тот экран, который видит человек, впервые открывший модуль.
  Передано окну шахмат вместе с поправкой; чинить не берусь — правка не
  однострочная, а `page.tsx` у них в активной работе.
- 19.08, замечено попутно: **сайт тоже выкатывают параллельно.** На проде
  фронт из чужой ветки, собран час назад; моя выкатка идёт следом. То есть
  гонка выкаток есть и на второй половине платформы, и протокола для неё нет —
  на бэкенде он появился сегодня, на фронте пока нет.
- 19.08, замечание к приёмке Б-2: проверка «рейтинг без выдуманных игроков»
  отвечает «да» при **нуле записей**. Формально это правда — подставных
  игроков нет. Но на запуске 30 августа таблица лидеров будет пустой, и первый
  посетитель увидит пустоту там, где обещано соревнование. Это не блокер и не
  дефект кода: либо принять как есть (честно), либо к запуску завести живые
  партии. Решение продуктовое.
- 19.08, состояние очереди на конец дня: страница, посадочная и приём адресов
  есть у CyberChess, IP Bureau, DevHub и Multichat. Единственный пробел —
  **QRight**: у него нет своей посадочной и нет поля приёма адресов на
  странице модуля. Пару закрывает `/bureau/launch`, но человек, попавший
  прямо на `/qright`, оставить адрес не может.
- 19.08, правило про выкатку сайта внесено в общие (`~/.claude/CLAUDE.md`,
  §13в): у платформы два прода, защита есть только у бэкенда, а у сайта нет —
  `vercel-deploy.sh` проверяет лишь, не пропадут ли страницы. До появления
  проверки в самом скрипте порядок такой: спросить `build.commit` у
  `/api/health` сайта и убедиться `git merge-base --is-ancestor <он> HEAD`.
  Признак — предок, а не имя ветки. Предупреждение положено и в каталог
  заданий (`frontend-deploys.md`) — сайт выкатывает не одно окно.
- 19.08, **решение по выкатке сайта: отдал работу тому, кто его выкатывает.**
  Дважды пробовал выкатить сам и дважды упёрся в гонку: пока идёт проверка
  типов (фронт большой, на машине пять параллельных сборок), у соседнего окна
  появляются новые коммиты. Соревноваться за один Vercel бессмысленно.
  Моя ветка вобрала всю их работу (`rev-list` до их ветки — 0, типы чистые),
  и им передана просьба сделать `git merge launch/2026-08-30` перед следующей
  выкаткой — ровно тот протокол, что сегодня сработал на бэкенде с шахматами.
  От окна запуска там всего два файла: честный текст про Bitcoin на `/qright`
  и плашка установки, убранная с посадочной.
  Сайт при этом уже в порядке: соседнее окно выкатило его в 08:25 и отметка
  вернулась (`build.branch` снова назван), «unknown» после моей прерванной
  выкатки продержался около получаса.
- 19.08, **дыра закрыта технически, а не на словах:** `vercel-deploy.sh` теперь
  сам спрашивает `/api/health` сайта и останавливается, если та сборка не лежит
  в истории вашей ветки. Признак — предок, а не имя. Три исхода: внутри истории
  (можно), чужое (стоп), нет отметки или коммит неизвестен локально (не знаю,
  тоже стоп). Обход — `ALLOW_OVERWRITE=1`, как у бэкенда.
  Проверено на всех четырёх ветвях по отдельности, включая ту, ради которой всё
  и делалось: чужой коммит даёт код 1 и «СТОП».
  Заодно остановлены два процесса выкатки, переживших остановку задачи, — они
  дошли бы до отправки и выкатили несогласованное. Это и есть та точка
  невозврата, о которой запись в памяти.
- 19.08, **последний пробел таблицы закрыт в коде:** у QRight появился приём
  адресов. Он был единственным модулем очереди без него — посадочная у пары
  QRight + Бюро одна (и это правильно), но попавший прямо на `/qright` уходил
  ни с чем. Метка источника «qright» уже есть в таблице модулей письма, то есть
  подписавшемуся уйдёт письмо про IP Bureau с датой 6 сентября, а не общее
  платформенное. tsc чистый.
  **На сайте появится после следующей выкатки** — её ведёт соседнее окно,
  моя ветка ему передана.
- 19.08, **письмо на запуск шахмат готово черновиком** (`запуск-материалы/`,
  копия в `Desktop\АЕВИОН\01-CyberChess\`). Пишется заранее, чтобы 30 августа
  не сочинять наспех. Отправляет основатель.
  Каждое утверждение — из сегодняшней проверки фактом: партия со Stockfish 18 и
  определением дебюта (играл руками), 502 584 задачи (замер по API), задача дня
  из настоящего банка, цена $19 (открывал кассу браузером).
  Отдельным разделом выписано, **чего в письме нет и почему**: турниры и
  рейтинг CPI не проверял; числа с витрины расходятся с базой и ждут решения;
  сравнений с lichess/chess.com без замера не делаю; скидки не назначаю.
  Там же четыре пункта, которые надо проверить перед самой отправкой, включая
  размер списка против потолка Brevo в 300 писем в сутки.
- 19.08, **мобильная проверка закрыта — инструментом, который уже был.**
  Я записал «мобильную ширину проверить не удалось» и собирался изобретать
  способ; оказалось, есть задача `AEVION-MobileSmoke` с прогоном на viewport
  390 и отчётом в vault. Но в её списке не было **ни одной** страницы очереди
  запуска: только денежные и инвесторские. То есть посадочные, куда ведёт
  трафик роликов и которые откроют почти только с телефона, не проверялись
  вовсе. Добавил пять адресов (было 7, стало 12), прогон запущен.
  Урок тот же, что весь день: сперва искать существующий инструмент, потом
  писать новый.
- 19.08, **мобильная проверка прошла: 12 из 12, все пять посадочных чисты.**
  Замер по прогону: `/cyberchess/launch` — документ 390 при viewport 390,
  горизонтальной прокрутки нет, кегль 16px, заголовок «Открываем 30 августа».
  То же у `/bureau/launch`, `/devhub/launch`, `/multichat-engine/launch` и
  `/qright`. Открытый пункт «мобильная ширина не проверена» закрыт фактом.
  Вылезающие за экран элементы остались только у старых страниц `/acquire`
  (три div) и `/pricing` (таблица) — это не из очереди запуска.
  Мелочь на потом: первым интерактивным элементом посадочной телефон видит
  переключатель языка, а не форму приёма адресов.
- 19.08, конец окна: ветка `launch/2026-08-30` догнала прод (отставание 0),
  типы бэкенда чистые, всё на месте — фильтр витрины, письмо по модулям, форма
  на `/qright`. Сторож страниц за это время вырос до **46** адресов: соседнее
  окно добавило ещё три, мерж их подхватил. Ветка передана окнам шахмат и
  фронта; сайт выкатывают они, бэкенд — по договорённости через мерж.
- 19.08, конец дня — **что осталось на чужой стороне (проверено, не со слов):**
  сайт собран в 08:48 из `deploy/frontend-all-2026-08-18`, мою ветку там ещё не
  вобрали (`merge-base --is-ancestor` — нет, формы на `/qright` в их версии 0).
  Значит три фронт-правки окна запуска пока НЕ на сайте: честный текст про
  Bitcoin, плашка установки убранная с посадочной, приём адресов у QRight.
  Задание с командой (`git merge launch/2026-08-30`) лежит в
  `C:\Users\user\aevion-задания-от-окна-запуска\frontend-ls-devhub.md`.
  **Проверить завтра первым делом:** вобрали ли; если нет — напомнить, а не
  выкатывать самому (сегодня попытка выкатить самому дважды упёрлась в гонку).
- 19.08, просьба основателя: подтверждение владения сайтом для **TikTok
  Developers**. Сайт уже подтверждался этим способом раньше, поэтому сделано по
  существующему образцу: файл в `frontend/public/` с именем `tiktok<КОД>.txt` и
  содержимым `tiktok-developers-site-verification=<КОД>`, без BOM и без
  завершающего перевода строки — TikTok сверяет дословно. Прежний файл оставлен.
  Выкатка запущена; перед запуском проверено расхождение с окном, ведущим сайт:
  коммит сайта целиком внутри моей ветки, значит ничего чужого не снесётся.
  Адрес: `https://aevion.app/tiktokaMe5oI0gJnAFtzH1ytXmUA5pv09mQMGE.txt`
- 19.08, 🔴🔴 **домены-двойники: заявки уходили в чужую компанию.** Проверял
  canonical перед разговором про SEO и увидел на странице цен
  `og:url = https://aevion.io/pricing`. Домен проверен фактом — он **не наш**:
  там работает другая компания с тем же названием (schema.org «Aevion builds
  an AI-native personal operating system», контакт jonathan@aevion.io, MX на
  Google). Переменные `NOTIFY_EMAIL` и `FROM_EMAIL` на проде не заданы, значит
  работали запасные значения из кода — оба на `aevion.io`:
  внутренние уведомления о заявках (имя, почта, организация, страна, канал)
  уходили им, письма о покупке отправлялись от их имени.
  Плюс `aevion.com` — **выставлен на продажу** (редирект на биржу atom.com) и
  стоял в canonical и schema.org раздела QBuild.
  **Сделано:** запасной адрес уведомлений убран совсем (не задана переменная —
  уведомление не шлётся, пишется предупреждение, заявка сохраняется);
  отправитель возвращён на наш домен; 53 + 35 замен во фронте и бэкенде.
  **Моя ошибка в том же заходе, исправлена:** заменил `aevion.tech` как
  «мёртвый», а MX-проверка показала, что это **единственный наш домен с
  рабочей почтой**. Скрипт восстановления при этом прошёлся по 27 файлам,
  включая юридические. Откатил и вернул ровно два адреса, которые сам сломал.
  **За основателем:** verified-домен в Resend, куда слать уведомления, и MX
  для `aevion.app` — на нём напечатано 66 адресов, ни один не принимает письма.
- 19.08, ✅ **утечка остановлена, подтверждено фактом:** `/health` отвечает
  `emailSender: from "AEVION <noreply@aevion.app>"` вместо `hello@aevion.io`,
  прод на `e58af911f6d2`, ветка `launch/2026-08-30`. После выкатки: пробник
  враждебного ввода 5 OK / 0 FAIL, пять ключевых ручек 200, витрина реестра
  по-прежнему 10 — чужого не задето.
  **Три выкатки до этого сорвались, и причина была не в Railway:** из
  Bash-инструмента `railway` не достучится до API (таймаут даже на `whoami`),
  а из PowerShell тот же вызов отвечает сразу. `railway status` при этом
  обманывал — он читает локальный файл связи и до API не ходит. Запуск через
  `& "C:\Program Files\Git\bin\bash.exe" scripts/railway-deploy.sh` из
  PowerShell прошёл с первого раза. Записано в память.
- 19.08, **StartupX обещает защиту, которой нет.** Не трогал: файлы правят пять
  веток, у `IdeaCard.tsx` четыре разные версии. Передаю находку целиком.

  В `src/routes/startupExchange.ts` (около строки 318):

  ```ts
  const qrightObjectId: string | null = null;
  const qrightProtected = true; // we always produce a deterministic contentHash
  ```

  То есть **регистрации в QRight не происходит вовсе**, флаг зашит в `true`
  всегда, `qright_object_id` всегда `null`. Комментарий это признаёт:
  «reserved for a future direct integration».

  **Пояснительный текст страницы при этом ЧЕСТЕН** — «SHA-256 контент-хеш
  (QRight-совместимая схема)», «позже идею можно перенести в QRight-реестр без
  переподписи». Врут два коротких места, которые человек и читает:

  | где | сейчас | предлагаю |
  |---|---|---|
  | сообщение после подачи, `page.tsx:122` | «Идея #N **защищена** · sha256:…» | «Идея #N зафиксирована · sha256:…» |
  | значок, `IdeaCard.tsx:78` | «**QRight** · sha256:…» | «QRight-совместимый хеш · sha256:…» |

  Слово «защищена» и значок с названием реестра читаются как регистрация. Модуль
  называется «биржа ЗАЩИЩЁННЫХ идей» и стоит $29/мес — цена ошибки не в тексте,
  а в том, что основатель отдаёт идею, считая её зарегистрированной.

  **Второе, тише и тоже там же.** При недоступной базе идея уходит в `Map` с
  обрезкой до `MEM_MAX_IDEAS`, и ответ ОДИНАКОВ с успешной записью — включая
  `qrightProtected` и `contentHash`. Тот же класс, что LifeBox и доска
  концептов; лечится полем `storage: "db" | "memory"` в ответе.

  Проверял: 38 файлов имеют и базу, и память, 14 без признака в ответе. Из
  проверенных поимённо `qstore` и сбор адресов сделаны ПРАВИЛЬНО (возвращают
  `mode`/`storage`), сломаны были доска концептов, LifeBox и вот StartupX.
- 19.08, **🔴 QLearn: сертификаты исчезают при каждой выкатке.** Модуль продаётся
  за $15/мес и входит в Medium и Full. Не трогал — файл правят
  `feat/free-fleet-keys` и `fix/build-closed-vacancy-feed`, версии разные.

  **Таблицы сертификатов нет вовсе** — ни `QLearnCertificate` в коде, ни в
  схеме. Курсы, уроки и зачисления пишутся в Postgres, а сертификаты живут
  только в `memCertificates = new Map()` (строка 129).

  Что видит человек, оплативший модуль и прошедший курс:

  | запрос | после перезапуска |
  |---|---|
  | `GET /me/certificates` | **пустой список** |
  | `GET /enrollments/:id/certificate` | **404 «Certificate not found»** |

  Выкаток бэкенда сегодня было шесть.

  **Хуже потери — подмена.** `POST /enrollments/:id/complete` (строка 538) при
  отсутствии сертификата генерирует новый:

  ```ts
  completedAt: new Date().toISOString(),      // СЕГОДНЯ, а не когда прошёл
  certificateNumber: "AEVION-" + Date.now(),  // новый номер
  ```

  То есть распечатанный или отправленный работодателю номер перестаёт совпадать,
  а дата окончания курса подменяется сегодняшней. И `registerCertificateInQRight`
  зовётся снова — в реестре копится по регистрации на каждую выкатку, все с
  разными хешами и все про одно достижение.

  Третье, мелкое рядом с этим: ответ содержит `qrightRegistered: true`, а сама
  регистрация запущена через `void` без ожидания — успех объявляется до того, как
  он известен. Тот же класс, что зашитый `qrightProtected` в StartupX.

  **Как чинить, образец в репозитории.** `src/lib/conceptBoardStore.ts` создаёт
  свою таблицу на старте через `CREATE TABLE IF NOT EXISTS` — миграция не нужна.
  Для сертификата достаточно `id, enrollment_id UNIQUE, course_id, user_id,
  course_title, completed_at, certificate_number, qright_hash`. Уникальность по
  `enrollment_id` заодно закрывает повторную регистрацию в QRight.
- 19.08, **сторож страниц был зелёным, наблюдая меньше половины сайта.**
  Замер: под наблюдением 39 публичных страниц верхнего уровня из 92. Печатал
  «46/46 PASS» — цифра честная, но она про список, а не про сайт.

  Невидимыми были `/terms` и `/privacy` (их чинили в тот же день), `/support`,
  `/help`, `/data-deletion`, `/qstore`, `/status`, `/launch-status` и почти все
  страницы модулей. Для запуска 30.08 это существенно: шахматные страницы вы
  добавили, а страницы, куда человек попадёт из шапки и футера, — нет.

  **Ветка `fix/pages-smoke-coverage`** — один коммит поверх вашей вершины
  `launch/2026-08-30`. Добавлено 53 страницы, стало 101. Все проверены ПЕРЕД
  добавлением (200, >5000 байт, бренд на месте), прогон после правки —
  **101/101 PASS, код 0**. Сторож, который краснеет с первого дня, отключают в
  тот же день, поэтому список по факту.

  Начинал НЕ со своей версии: файл разошёлся в пяти ветках, ваша ближе всех к
  проду.
- 19.08, **проверка денежного пути не запускалась ни разу.**
  `checkout-rails-prod-smoke.js` проверяет чекаут по шести сочетаниям
  тариф×период, живость вебхуков PayBox и PayPal, отклонение поддельной подписи
  и деградацию в каскад по умолчанию. Во всём репозитории НОЛЬ упоминаний файла,
  кроме него самого: не в `all-smokes`, не в `.github/workflows`, не в задачах.

  Нашёл, задав сторожу касс вопрос «16 OK — из скольких». Каталог покрыт
  полностью, а тарифы (главная линия выручки) идут другим путём, и проверка
  этого пути лежала мёртвой.

  **Ветка `fix/checkout-smoke-never-ran`** — один коммит поверх вашей вершины.
  Перед добавлением прогнал, чтобы не завести вечно красного сторожа: напрямую
  17/17 PASS, через набор с `BASE=https://api.aevion.app` — PASS за 7.7 с.
  Ежедневный прогон зовёт именно так.

  Рядом ещё 14 смоук-файлов вне `all-smokes`, но у большинства свой запуск
  (`bank-prod-smoke`, `qbuild-prod-smoke` — в `daily-smoke.yml`). Полностью
  беспризорным был только этот; `paddle-prod-smoke` стоит проверить отдельно.
