// Этот конфиг существует, чтобы `npx vitest run` из КОРНЯ репозитория не мог
// тихо проверить не то. Он ничего не запускает — он останавливает.
//
// 10.08.2026. В корне нет ни vitest, ни конфига, поэтому npx подтягивал
// отдельный vitest во временный кэш и запускал его с настройками по умолчанию:
//
//   • собирал `frontend/e2e/*.spec.ts` — это Playwright, а не vitest; ~20 файлов
//     падали с «Playwright Test did not expect test.describe() to be called
//     here» на любом коде;
//   • гнал фронтовые тесты без `environment: "jsdom"`, без `setupFiles` и без
//     алиаса `@` из `frontend/vitest.config.ts` — ещё ~10 падений на коде,
//     который в своём раннере полностью зелёный (476/476);
//   • терял алиас `opentimestamps` из бэкендового конфига.
//
// Корневой прогон показывал 32 падения там, где реальных было 2 — обе давно
// задокументированные хроники (`04-Daily/test-suite-flakiness-2026-07-26.md`).
// Набор, красный всегда, не отличает регрессию от шума: на него ссылались как
// на пройденную проверку, а он ничего не проверял.
//
// Почему не `projects: ["aevion-globus-backend", "frontend"]` — пробовал, и это
// оказалось ХУЖЕ, а не лучше. Бэкенд так действительно поднимается со своим
// конфигом, а все 40 фронтовых файлов не стартуют вовсе: vitest из npx-кэша
// ищет `jsdom` рядом с собой, а не в `frontend/node_modules`
// (`Cannot find package 'jsdom'`). Итог выглядит аккуратно — «1546 тестов, 1
// падение» — и умалчивает, что фронтенд не выполнялся ни одной строкой. Это
// ровно тот обмен, которого мы избегаем: красный шум заменяется на тихую
// неполноту. Починить это можно только установкой vitest в корень, а установку
// зависимостей делаем не походя.
//
// Поэтому — отказ с указанием рабочей команды.
throw new Error(
  [
    "",
    "В корне репозитория тестов нет — и запускать их отсюда через vitest нельзя:",
    "он не увидит ни jsdom фронтенда, ни алиасов бэкенда, зато подберёт Playwright-спеки.",
    "",
    "Рабочие команды:",
    "  npm test                     — оба пакета, каждый своим конфигом",
    "  npm run test:backend         — только aevion-globus-backend",
    "  npm run test:frontend        — только frontend",
    "",
  ].join("\n"),
);
