import type { Metadata } from "next";

/* Запрет индексации: страница ДЕЙСТВИЯ над конкретным объектом — запрос нотариального заверения конкретного сертификата.
 *
 * На неё приходят СО СТРАНИЦЫ сертификата, нажав кнопку, а не из поисковой
 * выдачи. В поиске она соревновалась бы с самим сертификатом, показывая при
 * этом форму, а не содержание. Заголовок на экране — «Request Notarization».
 *
 * ОБРАЗЕЦ, найденный 30.08.2026 группировкой списка наследующих canonical по
 * разделам: рядом с каждым объектом живут страницы двух видов — сам объект
 * (сертификат, артефакт, документ) и ДЕЙСТВИЯ над ним. Первым, возможно,
 * нужен свой canonical: они и есть доказательство, которое показывают.
 * Вторым нужен запрет показа.
 *
 * `follow: true` намеренно: ссылки со страницы обходить можно, запрещён показ
 * самой страницы. */
export const metadata: Metadata = {
  robots: { index: false, follow: true },
};

export default function BureauNotarizeLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
