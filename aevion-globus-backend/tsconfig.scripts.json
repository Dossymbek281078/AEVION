{
  "_comment": [
    "Проверка типов для scripts/ — отдельным конфигом, потому что основной",
    "tsconfig.json собирает ТОЛЬКО src/ (rootDir: src, include: src/**), и",
    "скрипты в его область не попадали вовсе. Обнаружено 2026-07-27: `tsc",
    "--noEmit` молчал на заведомо неверном типе в scripts/. Первый же прогон с",
    "этим конфигом дал 5 настоящих ошибок, три из них — в свежем скрипте,",
    "который считался проверенным (одна убирала обязательное текстовое тело",
    "письма).",
    "Только проверка, без сборки: скрипты запускаются через tsx."
  ],
  "extends": "./tsconfig.json",
  "compilerOptions": { "rootDir": ".", "noEmit": true, "outDir": null },
  "include": ["scripts/**/*.ts", "src/**/*.ts"]
}
